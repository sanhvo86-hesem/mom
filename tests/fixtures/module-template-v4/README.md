# module-template-v4 fixtures

Additive fixtures for Step 9/10/11/12 prototype QA. These fixtures are not production data.

The fixture script `mom/scripts/portal/74-module-template-v4-fixtures.js` is dev/test only and must never be loaded by production `portal.html`.
