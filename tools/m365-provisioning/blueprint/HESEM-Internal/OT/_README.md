# OT — Operational Technology (air-gapped vault)

CNC controller backup, PLC ladder, CMM program backup, post-processor library,
kinematic XML, DNC server config. Strict permission: OT Admin + IT-SOC only.
