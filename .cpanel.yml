---
deployment:
  tasks:
    - export DEPLOYPATH=/home/pqobnire/qms.hesem.com.vn
    - /bin/cp -R * $DEPLOYPATH/
    - /bin/cp .htaccess $DEPLOYPATH/.htaccess
    # One-time migration: copy live data from qms-data-private back into in-repo qms-data
    - /bin/cp -n /home/pqobnire/qms-data-private/config/*.json $DEPLOYPATH/01-QMS-Portal/qms-data/config/ 2>/dev/null || true
