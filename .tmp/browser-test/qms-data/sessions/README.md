# Runtime directory

Thư mục này dành cho runtime artifacts của môi trường chạy (session / rate-limit state). Trong gói tài liệu kiểm soát, mọi file runtime phát sinh phải được loại bỏ trước khi phát hành; server sẽ tự sinh lại khi triển khai thực tế.
