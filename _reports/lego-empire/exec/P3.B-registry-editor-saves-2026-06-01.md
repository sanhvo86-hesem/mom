# Exec · P3.B — Registry editor SAVE actions (L2/L3/L4 + theme preset clone)
### Session BACKEND · worktree `mom-mstudio-backend` · nhánh `codex/mstudio-backend-p3b-20260601` · 2026-06-01

## Đổi gì
- `mom/api/services/DesignTokenCatalogService.php` — thêm: `cloneThemePreset`, `saveComponentContract`+`getComponentContract`, `listBlockContracts`(DB)+`getBlockContract`+`saveBlockContract`, `listModuleArchetypes`(DB)+`getModuleArchetype`+`saveModuleArchetype`, helpers `normalizeContractStatus`/`normalizeStringList`/`hydrateBlockContract`/`hydrateModuleArchetype`/`decodeJsonColumn`.
- `mom/api/controllers/GraphicsGovernanceController.php` — thêm: `themePresetClone`, `componentContractSave`, `blockContractList`, `blockContractSave`, `moduleArchetypeList`, `moduleArchetypeSave`.
- `mom/api/routes/graphics-governance-routes.php` — đăng ký 6 action_key mới.

## Contract (xem bảng đầy đủ trong CONTRACTS-MODULE-STUDIO.md → mục P3.B)
| action_key | bảng (authority) | mig |
|---|---|---|
| `graphics_component_contract_save` | graphics_component_contract | 148 |
| `graphics_block_contract_list/save` | graphics_block_contract | 261 |
| `graphics_module_archetype_list/save` | graphics_module_archetype | 262 |
| `graphics_theme_preset_clone` | graphics_theme_preset (base_ref) | 263 |

Read = `requireGraphicsRead`; write = `requireWriteRequest` (CSRF + write perm). Mỗi mutation ghi `audit_events` qua `graphicsAudit` (`graphics.{component_contract,block_contract,module_archetype,theme_preset}.{saved,cloned}`). Save trả **full row** (FE refresh inspector — đáp ứng yêu cầu A→B).

## Self-audit
- **SSOT / một write-path**: mỗi lớp 1 action_key → 1 service method → 1 bảng đã có (KHÔNG bảng song song). ✓
- **PDO $N trap**: WRITES dùng `?` + `pgParams()` (1-indexed) theo đúng pattern `saveThemePreset`; JSONB qua `json_encode`+`?::jsonb`; TEXT[] qua `toPgTextArrayLiteral`+`?::text[]`; BOOLEAN qua `'true'/'false'`+`?::boolean`. READS dùng `$N` (đã chứng minh chạy live). ✓
- **Bảng mới**: không (dùng 148/261/262/263) → table-registry.json không đổi. ✓
- **Upsert idempotent**: `ON CONFLICT (key) DO UPDATE` → save lại = cập nhật, không nhân bản. ✓
- **Validate**: status/category ép về enum hợp lệ; key sanitize regex; route_class/key bắt buộc (throw 4xx). ✓
- **Clone an toàn**: ép `is_default=false,is_builtin=false,status=draft`, `base_ref=source` → clone không chiếm default; trùng key → lỗi. ✓
- **RBAC**: read vs write tách `requireGraphicsRead` / `requireWriteRequest` (Author-gated). ✓

## Phản biện (try-to-refute)
1. **"Casts/column sai schema → fail runtime im lặng"** → chạy thử 3 upsert TRÊN LIVE schema (BEGIN…ROLLBACK): component/block/archetype đều `INSERT 0 1`, casts text[]/jsonb/boolean + cột đúng. **Refute thất bại.**
2. **"$N write no-op (graphics write trap)"** → dùng `?`+pgParams như saveThemePreset đã chạy production. Không dùng $N cho write. ✓
3. **"Clone ghi đè preset đang dùng"** → guard `getThemePreset(new_key)!==null → throw`; force non-default. ✓
4. **"FE phải sửa caller"** → action mới, không đổi action cũ; FE consume khi sẵn sàng (CONTRACTS). ✓

## Gate local
- `php -l` service + controller + routes → No syntax errors. ✓
- SQL upsert validate live (rollback) → 3/3 OK. ✓

## Verify sau deploy (mục dưới khi merge xong)
- [ ] deploy.yml xanh.
- [ ] save 1 throwaway component/block/archetype qua DB-level (đã rollback-test); sau deploy kiểm route đăng ký (action_key có trong router) — smoke OK.

## Phạm vi & hoãn có chủ đích
- Theme **scheduler active enforcement** (`graphics_theme_schedule` resolve day/night/andon theo thời gian) — HOÃN sang follow-up (logic time-resolution riêng); `graphics_theme_schedule_list` (read) đã có sẵn.
- KHÔNG đụng uom/mda. Migration: P3.B không thêm migration (dùng bảng đã có).
