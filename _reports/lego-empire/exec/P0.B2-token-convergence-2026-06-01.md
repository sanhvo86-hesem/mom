# Exec · P0.B2 — One-token-authority convergence (graphics_token_value SSOT)
### Session BACKEND · worktree `mom-mstudio-backend` · nhánh `codex/mstudio-backend-20260601` · 2026-06-01

## Đổi gì
- `mom/database/migrations/285_graphics_token_value_org_default_completeness.sql` (mới)
- `mom/api/services/DesignTokenCatalogService.php` — `snapshotEffective` completion-pass + comment authority
- `_reports/lego-empire/CONTRACTS-MODULE-STUDIO.md` (mới, append-only) — báo FE

## Contract (backend↔frontend)
- action `graphics_token_catalog_snapshot&color_mode=<mode>` → `GraphicsGovernanceController::tokenCatalogSnapshot` (`:459-481`) → `snapshotEffective($scope,$mode)` (`:163-205`). JSON shape không đổi: `{ ok, snapshot:{token_key→value}, color_mode, scope, _meta.authority }`.
- scope FE truyền = `{role, user, [tenant, environment]}`; `resolveScopeChain` append `organization:default` làm base (`:734-744`) → overlay org→…→user.

## Root-cause (đo live, không phải giả thuyết)
- `graphics_token_catalog` active = **143**; nhưng `graphics_token_value` org:default published: light **103**, dark **103**, high-contrast **81**, `high_contrast`(underscore) **22**, print 81.
- → **40 catalog key KHÔNG có value row** (control.height.standard, radius/space master, brand.primaryHover/Soft, semantic.color.role.*, status.*.soft, components.chip/kpi/table…). Lý do: mig 148:511-533 seed value cho catalog-tại-148; mig 213/230/261-265 + DCC THÊM catalog key nhưng KHÔNG re-seed value.
- → 22 row `dcc.header.*` dùng `color_mode='high_contrast'` (underscore) — `normalizeColorMode`/`COLOR_MODES` dùng `'high-contrast'` (hyphen) → snapshot high-contrast không với tới (latent bug).
- Hệ quả: với 40 key đó snapshot thiếu → `tokens.read` rơi `HmTheme.getDeep` (JSON) → JSON-as-authority, đúng "hai-authority".

## Fix
1. **Migration 285** — re-run đúng pattern backfill 148:511-533 (nhặt MỌI active catalog key, `ON CONFLICT DO NOTHING`), nguồn = catalog `default_*` (đã đúng SSOT sau 230/264/266), coalesce dark/hc/print→light khi NULL. Lấp toàn bộ gap. Atomic `BEGIN/COMMIT` (migrate.sh không bọc txn).
2. **Normalize** `high_contrast`→`high-contrast` (guard NOT EXISTS hyphen-twin → không vi phạm unique, giữ metadata gốc), chạy TRƯỚC backfill.
3. **`snapshotEffective` completion-pass** — mode non-light tự lấp key thiếu bằng baseline light (chống tái diễn class bug per-mode-gap cho catalog key tương lai); chỉ ADD key thiếu, không override mode-specific value.

## Self-audit (checklist)
- **SSOT**: nguồn = catalog default (DB authority), zero hand-typed value; org:default thành bản sao đầy đủ catalog → 1 authority. ✓
- **Idempotent/additive**: `ON CONFLICT DO NOTHING` + UPDATE guard NOT EXISTS → re-run no-op; không destructive reseed (live POSTGRES_ONLY). ✓
- **FK** `graphics_token_value.token_key→graphics_token_catalog`: mọi key insert đến TỪ catalog → thoả. ✓
- **PDO $N trap**: migration là SQL thuần (psql -f); code không thêm SQL mới (completion-pass tái dùng query đã chạy live). ✓
- **Bảng mới**: không (chỉ thêm row) → table-registry.json không cần đổi. ✓
- **a11y/contract**: shape JSON snapshot không đổi → FE không cần sửa caller. ✓

## Phản biện đa chiều (7 lăng kính — try-to-refute)
1. **Correctness/regression**: "Snapshot bổ sung làm consumer đổi giá trị?" → so 40 key: catalog default_light vs live JSON → **diverge=0** (5 match: control.height.standard/radius.master/radius.card/space.master/space.section; 35 absent-in-json). 35 key vắng JSON tiêu thụ qua CSS var ở render thật; `tokens.read` chỉ dùng ở editor/preview → bổ sung = governed value thay fallback ad-hoc, KHÔNG đổi render. **Refute thất bại → an toàn.**
2. **SSOT-purity**: đúng — kéo authority về graphics_token_value, JSON thành cold-start net.
3. **RBAC**: snapshot read-gated `requireGraphicsRead`; migration không đụng quyền. ✓
4. **Perf**: snapshot là beacon load-time, FE cache; completion-pass thêm 1 query light khi mode≠light — chấp nhận (mỗi load 1 lần). ✓
5. **A11y**: high-contrast mode nay phục vụ đủ token (trước thiếu 62 + 22 unreachable) → CẢI THIỆN.
6. **Graphics polish**: catalog default khớp production CSS (32/8/4/12, #0c4a6e family) → không nhảy.
7. **Contract integrity**: action/shape khớp 2 đầu; lỗi không im (FK enforce, ON CONFLICT tường minh). ✓

## Gate local
- `php -l DesignTokenCatalogService.php` → No syntax errors. ✓
- `php mom/tools/release/check_migration_drift.php` → **clean (260 files, offline)**. ✓

## Verify sau deploy ✅ (PR #146 merged cbabf7c68 · deploy run 26765767081 = success)
- [x] light/dark/high-contrast/print count = **143** (trước 103/103/81/81); `high_contrast` underscore = **0** (trước 22).
- [x] DB store đúng SSOT: `control.height.standard=32px`, `space.master=8px`, `radius.master=4px`, `brand.primary=#0c4a6e`, `semantic.color.role.quality=#dc2626`, `status.success.soft=#d1fae5`.
- [x] deploy.yml xanh (Data Schema smoke aligned — migration 285 applied live).
- Collision note: mda merge `284_item_revision_command_authority_closure.sql` trùng số → migration của tôi renumber 284→**285** trước merge (drift clean 261 files).

## Coordination
- KHÔNG đụng vùng uom/mda (migrations 272-283 đang active). Migration 285 chỉ đụng `graphics_token_value` + chuẩn hoá color_mode. Đã append CONTRACTS cho FE: được tin snapshot cho 143 governed key; CHƯA cắt fallback per-key toàn cục tới P0.B2b (catalog mở rộng ~180 key + css_variable theo CSS).
