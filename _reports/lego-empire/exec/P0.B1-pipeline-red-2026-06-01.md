# Exec · P0.B1 — Gỡ deploy pipeline RED (Data Schema live-DB smoke)
### Session BACKEND · worktree `mom-mstudio-backend` · nhánh `codex/mstudio-backend-20260601` · 2026-06-01

## Kết luận: ĐÃ RESOLVED bởi session mda (verify-only từ session này)

P0.B1 đặt ra 3 việc; cả 3 đã được session mda xử lý trước khi session backend này bắt đầu. Session này **verify** trạng thái, không sửa (tránh ghi đè vùng đang active của mda).

### (a) Chẩn đoán structural_drift / missing tables
- `gh run list --workflow=deploy.yml` → run mới nhất `26759111729` (PR #143 `Merge … mda-runtime-table-authority-fix`) = **success**, headSha `b5363b818` = `origin/main` HEAD.
- PR #139/#141/#142 (mda-runtime-schema-authority-fix) trước đó FAIL → mda iterate tới #143 xanh.
- Root-cause cũ: migrations runtime authority (mda/uom v5) thêm bảng mà `table-registry.json` chưa khớp → `DataSchemaService::getWorkspace` báo `missing_table_count`/`structural_drift`. Commit `c6e416d9f fix(mda): register runtime authority tables` đăng ký các bảng đó vào registry authority.

### (b) Regen table-registry / schema-snapshot cho bảng v5
- Đã do commit `c6e416d9f` (mda). `php mom/tools/release/check_migration_drift.php` → **clean** (259 files scanned, offline mode); chỉ còn 3 INFO prefix-collision historical đã được approve (108/115/188).

### (c) Migration idempotent drop orphaned constraint (fresh-clone fix)
- `grep uom_conversion_rule_lifecycle_status_check` → đã có trong `268_uom_v5_manifest_human_approval_lock.sql:67`:
  ```sql
  ALTER TABLE uom_conversion_rule DROP CONSTRAINT IF EXISTS uom_conversion_rule_lifecycle_status_check;
  ALTER TABLE uom_conversion_rule DROP CONSTRAINT IF EXISTS uom_cr_lifecycle_v3;
  ALTER TABLE uom_conversion_rule DROP CONSTRAINT IF EXISTS uom_cr_lifecycle_v5_manifest_lock;
  ALTER TABLE uom_conversion_rule ADD CONSTRAINT uom_cr_lifecycle_v5_manifest_lock CHECK (...);
  ```
  → fresh-clone không còn drift; constraint chuẩn hoá idempotent. Việc (c) trong prompt KHÔNG cần làm thêm.

## Verify live (VPS, peer-auth psql)
- `SELECT count(*) FROM schema_migrations;` → **260** rows applied.
- `pg_constraint WHERE conrelid='uom_conversion_rule' AND conname LIKE '%lifecycle%'` → chỉ còn `uom_cr_lifecycle_v5_manifest_lock` (orphaned `uom_conversion_rule_lifecycle_status_check` ĐÃ biến mất trên live).
- Deploy run headSha = origin/main HEAD = success → smoke `pending=0 / missing=0 / structural_drift=0 / db_target_status=aligned` đã PASS trên live.

## Coordination
- Migrations 272–283 (uom/mda runtime authority) là vùng ĐANG ACTIVE của session mda. Session backend này **không tạo migration nào trong vùng uom/mda**; migration tiếp theo của tôi (P0.B2 graphics token) bắt đầu ≥ 284 và chỉ đụng `graphics_token_*`.

## Trạng thái: ✅ pipeline GREEN cho mọi session. Không cần action từ session backend. Chuyển P0.B2.
