# Exec · P0.2 — DISCOVERY (live probe đổi bản chất P0.2)
### 2026-06-01 · probe trực tiếp eqms.hesemeng.com (logged in)

## Giả thuyết ban đầu (SAI một nửa)
Kế hoạch: P0.2 = "cắt fallback `HmTheme.getDeep` ở `00bb:141-149`" vì sợ hai-authority lệch + token trắng.

## Sự thật đo được (read-only, live)
- Backend snapshot `graphics_token_catalog_snapshot` = **353 token** (graphics_token_value KHÔNG rỗng).
- Snapshot **CÓ** các key consumer cần (`brand.primary`, `control.height.standard`, `density.controlH`…).
- **Snapshot và HmTheme ĐỒNG Ý** với nhau (cùng nguồn admin config). Nên:
  - Fallback-cut **KHÔNG** gây token trắng → **rủi ro THẤP**, không gấp.
  - Bệnh thật = **giá trị trong DB authority bị SAI:**

| token_key | giá trị DB live | SSOT đúng | nguồn SSOT |
|---|---|---|---|
| `brand.primary` | `#0c4f6e` | `#0c4a6e` | mig 264 + orders-v3 (typo a↔f) |
| `control.height.standard` | `24px` | `32px` | mig 230 (single-size) |
| `density.controlH` | `24px` | `32px` | mig 230 |

- CSS production render ĐÚNG (#0c4a6e / 32px qua orders-v3 default) → divergence là **CSS-default vs DB-token-value**; JS consumer đọc token nhận giá trị lệch (#0c4f6e, 24px). Audit P1-P4 đã đo nút Module Master = 32px live (khớp CSS, không khớp token).

## P0.2 ĐÚNG (đã hiệu chỉnh)
1. **P0.2a — đính chính giá trị DB (gấp, là bug thật):** migration idempotent UPDATE `graphics_token_value` (pattern mig 264): `brand.primary` #0c4f6e→#0c4a6e (mọi color_mode/scope='organization:default'); `control.height.standard` + `density.controlH` 24px→32px. Rà thêm các key brand.* / density.* lệch. Backup data-private trước.
2. **P0.2b — cắt fallback (rủi ro thấp, dọn code):** `00bb:145-148` bỏ nhánh HmTheme.getDeep; `HmTheme.getDeep` → read-adapter. Làm sau khi P0.2a xanh.
3. Verify Chrome: `tokens.read('brand.primary')`=#0c4a6e, `tokens.read('control.height.standard')`=32px; control render vẫn 32px; brand không nhảy.

## Rủi ro của P0.2a
- UPDATE live POSTGRES_ONLY → đổi giá trị runtime cho mọi user. NHƯNG visual-risk THẤP (CSS đã render #0c4a6e/32px; fix chỉ kéo DB token về khớp). PDO `$N` trap: service dùng $N qua DataLayer (đã chạy được) — migration SQL thuần thì OK.
- **Tạm dừng xin founder duyệt** vì đụng giá trị token LIVE (khác P0.1 chỉ là build artifact).

## Trạng thái
- P0.1 ✓ shipped+verified. P0.2 = mới phát hiện 2 bug data; chờ go để ship migration đính chính.
