# Exec · P0.1 — Tech base tokens (DISCOVERY + corrected deliverable)
### Nhánh `codex/p0-1-tokens-css-20260601` · 2026-06-01

## Phát hiện (đổi premise của P0.1)
Kế hoạch ghi P0.1 = "dựng pipeline DTCG→Style Dictionary→tokens.css". **Xác minh repo cho thấy nền này ĐÃ TỒN TẠI và đã wire CI:**
- `tokens/lego.tokens.json` — nguồn DTCG 2025.10 (Tier-1 seed → generator derive ramp).
- `tools/scripts/gen-lego-tokens.mjs` — pure-Node, OKLCH math, `@layer lego.tokens`, `@supports` OKLCH enhancement, light/dark, `--check` drift mode.
- Output: `mom/styles/lego-foundation.css` + `lego-shell.css` + `tokens/lego.tokens.generated.json` (đã nạp portal.html L24/L46).
- CI: `ci.yml:489-490` (`gen-lego-tokens --check`) + `deploy.yml:209` (`check_lego_tokens.php`).

→ "Dựng pipeline mới" sẽ tạo **hệ token thứ ba** = đúng vi phạm SSOT đang chống. **Bỏ.**

## Gốc F5 ở tầng nguồn token (defect thật)
`lego.tokens.json:8` seed `brand="#1565c0"` (cũ) → sinh `--lego-brand:#1565c0`, trong khi orders-v3/production + mig 264 dùng `#0c4a6e` (canonical). Đây là một authority còn lệch ở **tầng nguồn DTCG** — mig 264 chỉ sửa row DB, chưa đụng nguồn này.

## Đã làm (P0.1 đúng = align nguồn, không dựng hệ mới)
1. `tokens/lego.tokens.json`: brand seed `#1565c0 → #0c4a6e` (canonical).
2. Chạy `gen-lego-tokens.mjs` → regenerate `lego-foundation.css` (`--lego-brand:#0c4a6e` + `oklch(0.391 0.085 240.88)`; ramp base/hover/active/on từ #0c4a6e) + `lego-shell.css` + `lego.tokens.generated.json` (source-sha256 `d4baba5b`).

## Gate (tự kiểm)
- `gen-lego-tokens.mjs --check` → **in sync ✓**
- `php check_lego_tokens.php` → **PASS**
- SSOT: làm qua pipeline (sửa nguồn→regenerate), zero hardcode, không hệ thứ ba. ✓
- Hướng đúng: hoàn tất một phần "chốt #0c4a6e mọi tầng" (P0.3) ngay tại tầng nguồn DTCG.

## Trạng thái / chưa làm
- Commit + push nhánh codex (chỉ chạy CI, **chưa deploy live** — deploy = merge main).
- CHƯA: verify Chrome trên VPS (chờ deploy).
- **Tạm dừng báo founder** vì: premise P0.1 đã đổi (nền đã có) → P0 thu gọn về **convergence** (P0.2 cắt fallback HmTheme + hợp nhất `--lego-*` ↔ `--o3-*`/`--brand`); bước kế (P0.2) là live-backend rủi ro cao.

## Đề xuất P0 đã hiệu chỉnh
- P0.1 ✓ (nguồn DTCG #0c4a6e).
- **P0.2** (rủi ro cao, live): cắt fallback `00bb:141-149`; HmTheme.getDeep→read-adapter; backfill `graphics_token_value` scope=global; hợp nhất bridge `--lego-*`↔production.
- **P0.3** dọn nốt #1565c0 JS default còn ở `00b:1179`, `00d:275/284`.
- **P0.4** bỏ Custom CSS. **P0.5** CI no-hardcode mở rộng.
