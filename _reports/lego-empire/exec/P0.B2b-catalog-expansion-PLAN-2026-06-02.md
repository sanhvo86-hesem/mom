# Exec · P0.B2b — Catalog expansion to retire the per-key JSON fallback (PLAN, not auto-executable)
### Session BACKEND · 2026-06-02 · scoped for a curated follow-up phase (NOT this branch)

## Mục tiêu
Cho FE cắt fallback per-key `00bb:145` HOÀN TOÀN: mọi token_key FE đọc qua `tokens.read` phải có trong snapshot (graphics_token_value) → cần catalog row (FK) + value row cho mọi key đó.

## Gap đo được (live, 2026-06-02)
- JSON authority (`design-system-config.json`) flatten = **356 leaf**; fold colorsDark→colorsLight = **340**.
- `graphics_token_catalog` = **154**. **GAP = 260 key**.
- Theo namespace: `components` 101 · `eqms` 89 · `moduleMaster` 19 · `layout` 13 · `density` 6 · `statusColors` 6 · `statusColorsDark` 6 · `colorsLight` 5 · `v4Surfaces` 4 · `effects` 3 · `advanced` 2 · `typography` 2 · `fontSize` 1 · `motion` 1 · `shadows` 1 · `moduleOverrides` 1.

## TẠI SAO KHÔNG auto-bulk-generate (bẫy đã xác minh)
1. **Namespace divergence catalog ↔ JSON** — KHÔNG fold cơ học được:
   - JSON `statusColors.*` / `statusColorsDark.*` ↔ catalog dùng `status.*` (đã có `status.success.light`…). Auto-add `statusColors.*` → tạo key DUPLICATE divergent.
   - JSON `fontSize.*` ↔ catalog `fontScale.*`.
   - JSON `colorsDark.*` fold vào `colorsLight.*` dark column (đúng), nhưng phải biết quy ước.
2. **Module-specific KHÔNG phải token toàn cục:** `eqms.*` (89), `moduleMaster.*` (19), `moduleOverrides.*` (1), `v4Surfaces.*` (4) = config riêng module/eqms-suite, KHÔNG nên vào catalog L0 toàn cục (sẽ làm bẩn token list của Theme editor + sai bản chất scope). → ~113 key NÊN ở scope=module, không phải global catalog.
3. **css_variable phải khớp CSS thật** — không suy diễn được tin cậy (vd `components.table.cellFontSize`→`--o3-table-cell-size`, drop "Font"; `paddingX`→`pad-x`). Sai → Theme editor bind vào CSS var ma. (css_variable nullable nên có thể để NULL, nhưng vẫn cần khi editor bind.)

## Kế hoạch curate (per-family, có review)
1. **Global design-token families** (~127 key, an toàn governance): `components.*` (1:1 namespace, đã có chip/kpi/table → thêm btn/badge/breadcrumb/card/dropdown/empty/field/flow/modal…), `layout.*`, `colorsLight.*` (5 còn lại + map colorsDark dark), `effects.*`, `typography.*`, `motion.*`, `shadows.*`. Mỗi key: value_type suy từ value (hex/rgba/px/unitless/keyword/ms), default_light/dark từ JSON (fold), css_variable **sourced từ CSS** (grep `--o3-*` / orders-v3.css để khớp), KHÔNG suy diễn.
2. **Family cần ánh xạ tên** (review): `statusColors`→`status` (kiểm trùng key đã có), `fontSize`→`fontScale`, `density.*` (map vào control/space token đã chuẩn hoá mig 227/230 — có thể đã cover, chỉ alias). Quyết định: alias hay key mới.
3. **Module-specific** (`eqms`/`moduleMaster`/`moduleOverrides`/`v4Surfaces`): KHÔNG vào global catalog. Để FE đọc qua scope=module (graphics_token_value scope='module:<id>') hoặc giữ HmTheme fallback. Cần quyết định kiến trúc (founder).
4. Sau khi catalog phủ đủ global key + seed value rows (re-run pattern mig 285): FE cắt fallback per-key cho global keys; module keys theo (3).

## Cơ chế thực thi (khi curate xong)
- Migration: INSERT catalog rows (idempotent ON CONFLICT DO NOTHING) với css_variable đã verify từ CSS + default từ JSON fold.
- Re-run org:default value backfill (copy mig 285:2a-2d) → seed value rows mọi mode.
- Verify harness: snapshot count tăng đúng, value khớp JSON, css_variable khớp CSS.
- FE: cắt nhánh HmTheme.getDeep ở 00bb cho nhóm key đã phủ.

## Trạng thái HÔM NAY (đủ để FE tiến)
Sau fix `$N` (PR #159), snapshot ĐÃ phục vụ **143 governed catalog key từ DB**. FE **được phép tin snapshot + cắt fallback cho 143 key đó** ngay. 260 key còn lại giữ HmTheme fallback tới khi phase curate này chạy. Không có lỗi thị giác (CSS render qua HmTheme độc lập).
