# Exec · P1.1 — Rename "Master Module Template" → "Module Studio"
### Nhánh codex/p1-rename-freeze-lift-20260601 · 2026-06-01

## Founder authorization
Founder (chủ repo) chính thức cho phép sửa forbidden files (ADR-0004) cho chương trình Module Studio: "edit những file liên quan để chạy liên tục cho đến khi hoàn tất."

## Phát hiện về CI guard (Tường B không cản)
ci.yml "Forbidden diff guard" CHỈ fail khi diff đụng file HMV4 slice (70-74-module-template-v4…) ĐỒNG THỜI đụng forbidden file. PR rename không đụng file 70-74 → guard short-circuit "PASS". critical_portal_scripts chỉ check conflict-marker/fixture-ref. Nên rename 02-state-auth-ui.js pass CI.

## Đã làm
- `02-state-auth-ui.js`: nav item (785) labelEn/Vi → 'Module Studio' + note mô tả nhiệm vụ; nav-section (3153) "TEMPLATE LAB" → "MODULE STUDIO", button → "Module Studio"; page title (3393) → 'Module Studio'. Giữ id `template-demo` (routing/permission key bất biến).
- `docs/adr/0004-forbidden-file-list.md`: amendment ghi nhận Module Studio program là exception founder-approved (không nới lỏng bảo vệ HMV4-slice).

## Gate
- `node --check 02-state-auth-ui.js` OK; 0 "Master Module Template" còn lại; builder (31) không có header riêng.
- Forbidden-diff guard: PASS (không kèm file HMV4 slice).

## Verify (sau deploy / static JS)
- Sidebar: section "MODULE STUDIO" + item "Module Studio"; breadcrumb title "Module Studio".
- Lưu ý: deploy pipeline còn RED (Tường A cross-session) — static JS vẫn serve rename qua nginx.
