# Exec · P4.B — Build Packet schema + CI manifest gate (SSOT by machine)
### Session BACKEND · worktree `mom-mstudio-backend` · nhánh `codex/mstudio-backend-p4b-20260601` · 2026-06-01

## Đổi gì
- `mom/contracts/module.build-packet.schema.json` (mới) — JSON Schema draft-07 cho L5 manifest canonical: `moduleId` + `moduleArchetype` + `themePresetKey` + `zones{zone:[block_key | {block,slots,data,a11y}]}`, zero style.
- `mom/tools/release/check_module_manifest.php` (mới) — gate quét `mom/design/build-packets/*.json`.
- `.github/workflows/ci.yml` — thêm step "Module build-packet manifest gate" vào job `graphics-safety`.
- `tools/scripts/ci/smart-classify.sh` — `needs_graphics_safety=true` khi đụng build-packets / schema / checker.

## Rule gate (CHECK 1–5)
1. JSON well-formed.
2. Required: `moduleId`, `moduleArchetype`, `zones`.
3. **Zero style** (cốt lõi): reject hex `#rrggbb`, `<n>px`, inline `"style"` key, raw HTML tag trong string, `density` override numeric/px (phân biệt `densityModes` enum — KHÔNG flag).
4. Mọi **dotted block_key** (`a.b`) phải published trong L3 registry (`00bc-block-registry.js`). Instance-id `blk-…` packet-local → advisory.
5. Advisory: thiếu `a11yProfile` / `previewScene`.

## Grandfather (không block cây hiện tại)
- Allowlist: `M2-orders.json`, `M4-purchasing.json` → mọi finding downgrade ADVISORY, exit 0.
- Packet MỚI / de-listed → full P0 bar. "Thu allowlist dần" làm gate siết tự động.

## Self-audit + phản biện (try-to-refute)
1. **"Gate block deploy cây hiện tại"** → chạy local: 2 packet hiện có → 0 blocking, exit **0** (chỉ advisory a11yProfile). **Refute thất bại.**
2. **"Gate không thực sự enforce (no-op)"** → test packet bẩn `__test_bad.json` (hex+px+style+phantom block) → **4 P0, exit 1**; `table.data` (published) pass. Enforce thật. **Refute thất bại.**
3. **"density false-positive"** → `densityModes` enum array KHÔNG match regex `"density":\s*"?\d`. ✓
4. **"Gate không chạy khi manifest đổi"** → `smart-classify` test: build-packets / schema / checker đều `needs_graphics_safety=true`. ✓
5. **"Phá classifier self-test"** → `test-smart-classify.sh` → **PASSED**. ✓

## Gate local
- `php -l check_module_manifest.php` → OK. Run gate → 0 blocking, exit 0.
- Dirty-packet test → exit 1 (đúng).
- `bash -n smart-classify.sh` OK; classifier self-test PASSED.

## Verify sau deploy (mục dưới)
- [ ] CI `graphics-safety` job có step "Module build-packet manifest gate" và xanh trên PR.

## Phạm vi
- CI-only, không write live, không migration. Rủi ro thấp nhất. KHÔNG đụng uom/mda.
- Theo dõi: khi P4.3 migrate module sang manifest, thu dần grandfather allowlist.
