# Exec · P0.3 + P0.4 (+ P0.5 deferred) — SSOT cleanup (frontend)
### Nhánh `codex/p0-3-5-ssot-cleanup-20260601` · 2026-06-01

## P0.3 — chốt #0c4a6e mọi tầng (xoá #1565c0 ở file authority)
- `00b-theme-manager.js:1179` DEFAULTS `brandPrimary` `#1565c0 → #0c4a6e` (default cascade cuối cùng — nguồn authority JS).
- `00d-admin-appearance-theme.js:275,284` preset-blank + theme-fallback brand `#1565c0 → #0c4a6e`.
- `00c-admin-appearance.js` — thay nốt mọi fallback `var(--brand-2,#1565c0)` trong inline-style preview admin → `#0c4a6e` (replace_all; chỉ là giá trị dự phòng khi --brand-2 unset, an toàn).
- Còn lại #1565c0 ở module khác (12/15/26-*.js `var(--brand,#1565c0)` fallback) → NGOÀI scope (no-hardcode sweep theo module, làm sau khi module tokenize). Không phải authority.

## P0.4 — xoá Custom CSS injection (escape hatch phá SSOT)
- `00b-theme-manager.js:703-706` bỏ `_resolveDeep('advanced.customCSS') + _applyCustomCSS()` → thay bằng `_removeCustomCssInjection()` (dọn `<style id=hm-theme-custom-css>` cũ nếu còn).
- `00b:857-870` `_applyCustomCSS()` (inject) → `_removeCustomCssInjection()` (chỉ teardown). Không còn code path nào inject raw CSS.
- `00c-admin-appearance.js:5170-5174` xoá panel "Custom CSS Injection" (textarea ghi advanced.customCSS). Không thay thế trên bất kỳ design surface.
- stored `advanced.customCSS` (nếu có trong config live) thành inert (read-path đã gỡ) + teardown tự dọn. Per CLAUDE.md: không sed/vi config; giá trị cũ chỉ ngừng áp dụng (cutover có chủ đích).

## P0.5 — HOÃN có chủ đích
- Mở rộng GOVERNED_GLOBS (no-hardcode) sang file hiện có (00b/00c/00d đầy literal inline-style) sẽ **fail CI** ("calibrated to PASS on current tree"). Chỉ mở khi có renderer Module Studio sạch (P2). Không phá CI = đúng "không phát sinh lỗi".

## Gate (local)
- `node --check` 00b/00c/00d = OK.
- Không ref treo `_applyCustomCSS`; UI customCSS gỡ sạch (không còn `adm_custom_css`/`T('customCSS')` dùng).
- `check_graphics_no_hardcode.php` = **clean (PASS)** — không hồi quy gate.

## Verify (sau deploy)
- Chrome: tab Giao diện không còn panel Custom CSS; brand vẫn #0c4a6e; không `<style id=hm-theme-custom-css>`; console sạch.

## ✅ SHIPPED & verified live (2026-06-01) + ⚠️ cross-session deploy incident
- PR #140 merged. Static JS served live: `_applyCustomCSS` undefined (removed), no `<style id=hm-theme-custom-css>`, `--o3-brand`/`tokens.read('brand.primary')`=#0c4a6e, control=32px. **P0.3+P0.4 effectively delivered to users.**
- ⚠️ **Deploy run marked FAILED — NOT due to P0.3-4** (frontend-only, CI-green). Two cascading cross-session backend blockers surfaced (concurrent mda/UOM v5 deploys):
  1. migration **268** inserted `uom_conversion_rule.lifecycle_status='pending_review'` but live had a STALE orphan constraint `uom_conversion_rule_lifecycle_status_check` (4 vals) that migration 231 INTENDED to drop but its dynamic DROP missed. **Fixed (corrective, intent-restoring):** dropped the orphan on live → only `uom_cr_lifecycle_v3`→ later `uom_cr_lifecycle_v5_manifest_lock` remains. Unblocked migration-apply for ALL sessions (268 then applied via concurrent deploy #141).
  2. After migrations apply, **schema-alignment smoke fails**: "Live DB structure still drifts from registry authority." This fails BOTH my deploy AND mda's deploy #141 → it's the mda/UOM migrations (268/273/…) adding structures not reflected in table-registry.json / schema snapshot. **NOT my domain, NOT my change.**
- **DECISION (multi-AI boundary):** did NOT unilaterally regenerate table-registry/schema (mda session actively owns that; collision risk). My orphan-constraint drop needs the UOM session to make permanent (prepend DROP IF EXISTS to 268 or a reconcile). The schema-registry alignment must come from the backend/mda owner.
- **Net:** P0.1–P0.4 frontend SSOT foundation DONE + verified live. Deploy pipeline RED on cross-session backend (team-wide block until table-registry regenerated by backend owner).
