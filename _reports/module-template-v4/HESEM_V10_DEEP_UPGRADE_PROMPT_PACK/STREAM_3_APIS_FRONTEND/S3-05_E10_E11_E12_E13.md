# S3-05 — E10 Notification + E11 Bulk + E12 File + E13 LRO

```
prompt_id: S3-05    stream: 3    sequence: 5 of 12    effort: ~80 min
```

## Pre-flight reading

```
1. S3-00 stream master
2. V9: E10_NOTIFICATION_API.md, E11_BULK_OPERATION_API.md,
   E12_FILE_UPLOAD_API.md, E13_LONG_RUNNING_OPERATION_API.md
3. Cross-refs: H1 §3 (regulator window enforcement); E15 (webhook
   delegate); H4 EC-22; H5 retention; I7 §7 (sub-processor;
   AV provider; signing); per pack J1-J5
4. Standards: OpenAPI 3.1.1; AsyncAPI 3.0; CloudEvents 1.0;
   HTTP 207 Multi-Status (RFC 4918); Google AIP-151 LRO;
   NDJSON / CSV / Parquet streaming; S3 pre-signed URL; ClamAV;
   per pack content inspection (ITAR; biocomp; FAI bubble)
```

## Deliverable

```
PART_E_API_CATALOG/E10_NOTIFICATION_API.md
PART_E_API_CATALOG/E11_BULK_OPERATION_API.md
PART_E_API_CATALOG/E12_FILE_UPLOAD_API.md
PART_E_API_CATALOG/E13_LONG_RUNNING_OPERATION_API.md
```

## Depth requirements

Each chapter: full per-endpoint contract per S3-00.

E10: ≥ 11 endpoints (preferences; inbox; mark read; broadcast;
delivery audit; template; regulator-window enforcement; mute;
per-pack; vendor cross-tenant); per-channel SLO; quiet-hours
bypass for SEV-1; per-tenant freeze interaction.

E11: ≥ 7 endpoints (import; export; bulk command 207; status;
restart; per-pack); per-record idempotency; HTTP 207 multi-
status; signed manifest output; per-pack overlay (DSCSA bulk;
PPAP per-part batch; FAI bulk; UDI bulk; §204 bulk).

E12: ≥ 7 endpoints (initiate; confirm; download; scan status;
metadata; retention; per-pack content inspect); per-pack
overlay (ITAR watermark; biocomp parse; FAI bubble extraction
per AI-08; CCP photo; thermal cycle data); WORM lock per H5;
virus + content inspection per I7.

E13: ≥ 7 endpoints + ≥ 22 operation types registry (audit
pack export; DSAR; OTG replay; mock recall; PSUR / APR draft;
multi-tenant migration; GUDID + EUDAMED batch; GIDEP batch;
FSMA §204 export; ICSR bulk; PCCP rebuild; etc.); checkpoint
+ restart; per-tenant queue; SLO-11 acknowledge.

## Required substance

E10: ≥ 4,500; E11: ≥ 5,000; E12: ≥ 5,000; E13: ≥ 5,000 words

## Acceptance criteria

```
[ ] Per-endpoint full contract per chapter
[ ] Per-pack overlay each chapter
[ ] Regulator-window enforcement (E10) + per H1 §3
[ ] Per-record idempotency (E11)
[ ] WORM + virus + ITAR watermark (E12)
[ ] LRO operation types registry ≥ 22 (E13)
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted per chapter + cumulative
```

## Decision phrase

```
S3-05_E10_E11_E12_E13_DEEP_UPGRADE_COMPLETE
```

After: load `S3-06_E15_INTEGRATION.md`.
