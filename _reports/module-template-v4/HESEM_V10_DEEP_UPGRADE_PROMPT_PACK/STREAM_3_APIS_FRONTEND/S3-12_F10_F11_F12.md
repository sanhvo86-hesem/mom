# S3-12 — F10 Design Tokens + F11 Accessibility + F12 i18n

```
prompt_id: S3-12    stream: 3    sequence: 12 of 12 (final S3)
effort: ~80 min
```

## Pre-flight reading

```
1. S3-00 stream master
2. V9: F10_DESIGN_SYSTEM_AND_TOKENS.md (already substantive),
   F11_ACCESSIBILITY.md (already substantive at 418 lines),
   F12_INTERNATIONALIZATION.md
3. Cross-refs: F0..F9; ADR-0009 (Graphics Authority); per-pack
   J1-J5; per regulator language
4. Standards: WCAG 2.2 AA + AAA (per F11); WAI-ARIA 1.2;
   EN 301 549; ADA + Section 508; EAA 2025; AODA;
   IEC 62366 (MD usability); ICU MessageFormat 2; CLDR;
   IANA tz; W3C i18n Working Group
```

## Deliverable

```
PART_F_FRONTEND_CATALOG/F10_DESIGN_SYSTEM_AND_TOKENS.md
PART_F_FRONTEND_CATALOG/F11_ACCESSIBILITY.md
PART_F_FRONTEND_CATALOG/F12_INTERNATIONALIZATION.md
```

## Depth requirements

F10:
- Design Tokens authority model (per ADR-0009; per-tenant
  override; preview / simulation per ControlKit)
- Token catalog (color; spacing; radius; font; motion;
  shadow; tier-thresholds; per-state visual)
- Per-pack visual overlay (Pharma cleanroom-friendly;
  Aero ITAR watermark; Auto OEM brand-mirror; MD device-class
  signaling; Food allergen-warning palette)
- Dark mode + density variants
- No-hardcode CI gate (per ADR-0009 + per I1)
- Token migration discipline

F11 (already deep; expand specific deeper):
- WCAG 2.2 AA (per SC) + selective AAA (per regulated tenant)
- WAI-ARIA 1.2 dialog / wizard / tabpanel patterns
- IEC 62366 usability (MD) — formative + summative testing
- Per-pattern technique catalog (per F0..F8)
- Test matrix (axe-core CI; manual screen reader NVDA/JAWS/
  VoiceOver/TalkBack; manual keyboard; manual dark-mode +
  density; manual zoom 200%; mobile + tablet; annual third-
  party audit; per-pack)

F12:
- ≥ 25 locale catalog (en/vi/ja/zh-CN/zh-TW/ko/de/es/pt/fr/it/
  nl/pl/ru/ar (RTL)/he (RTL)/hi/id/th/tl/ms/tr/sv + per
  customer demand)
- ICU MessageFormat 2 + CLDR + IANA tz
- Per-pack regulator-required language (Pharma EU MS; MD
  EU MDR Annex II patient/user instructions; FALCPA / EU
  1169/2011 food labels; ITAR English-only Aero export-control)
- Per-tenant terminology overrides per CSR (per H1 §7)
- RTL discipline (Arabic / Hebrew; CSS logical properties)
- Pseudo-localization test mode
- Translation workflow per K3 partner
- Per-language a11y (lang attribute per page + region; LSF
  for voice output; IEC 62366 per language for MD)

## Required substance

F10: ≥ 4,500; F11: ≥ 4,000 (V9 already 418 lines; expand);
F12: ≥ 4,500 words

## Acceptance criteria

```
[ ] F10 token catalog substantive; per-pack visual overlay
[ ] F11 per-SC compliance (every WCAG 2.2 AA + selected AAA SC)
    + IEC 62366 (MD)
[ ] F12 ≥ 25 locales + per-pack regulator-required language
[ ] Pseudo-localization test mode
[ ] No-hardcode CI gate per ADR-0009
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase

```
S3-12_F10_F11_F12_DEEP_UPGRADE_COMPLETE
```

After: stream 3 complete. Emit:
```
STREAM_3_APIS_FRONTEND_DEEP_UPGRADE_COMPLETE
```
