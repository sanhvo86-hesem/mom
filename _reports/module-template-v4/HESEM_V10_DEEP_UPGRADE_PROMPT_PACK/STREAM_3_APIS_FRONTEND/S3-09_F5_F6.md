# S3-09 — F5 Authoritative Record Shell + F6 Action Console

```
prompt_id: S3-09    stream: 3    sequence: 9 of 12    effort: ~80 min
```

## Pre-flight reading

```
1. S3-00 stream master
2. V9: F5_AUTHORITATIVE_RECORD_SHELLS.md,
   F6_ACTION_CONSOLES.md
3. Cross-refs: F0/F4/F7/F8; E3 + E5 + E7 + E8 + E11; per-pack
   J1-J5; L1 (banned-decision UI; friction calibration per L1 §6)
4. Standards: WAI-ARIA 1.2; WCAG 2.2 AA; ICU MessageFormat 2;
   IEC 62366 (MD usability); ISA-101 HMI
```

## Deliverable

```
PART_F_FRONTEND_CATALOG/F5_AUTHORITATIVE_RECORD_SHELLS.md
PART_F_FRONTEND_CATALOG/F6_ACTION_CONSOLES.md
```

## Depth requirements

F5:
- 7-tab AR shell (Overview / Detail / History / Activity /
  Linked / Evidence / Signatures + per-pack additional)
- ≥ 50 record-shell instances (per all roots in M3 × per-pack
  overlay) — at least catalog them with pointer to per-domain
  spec
- Per-state UI (loading / empty / partial-access / degraded /
  live-mode / fixture-mode banner per ADR-0001)
- Per-tab freshness SLO target
- Conflict-resolution UI (ETag mismatch flow)
- Per-pack overlay substantive (DHF tabs MD; EBR tabs Pharma;
  PPAP tabs Auto; FAI tabs Aero; HACCP tabs Food)

F6:
- ≥ 19 AC catalog (NC bulk; CAPA bulk; dispatch console;
  recall execution; HACCP CCP; DSCSA suspect; GIDEP draft;
  vigilance triage; complaint console; deviation console;
  LPA audit; SCAR; etc.)
- Per-AC contract (selection from F4 → confirmation → evidence
  capture → bulk authority sign per E7 → bulk submit per E11
  → multi-status per-record result render)
- Per-record L1 enforcement (bulk does not bypass per-record
  banned-decision)
- Live stream pattern (per E5 §2.4)
- Per-pack live consoles
- Per-pack overlay substantive

## Required substance

F5: ≥ 6,000 words; F6: ≥ 5,000 words

## Acceptance criteria

```
[ ] F5 ≥ 50 record-shell instances catalogued
[ ] F6 ≥ 19 AC catalog
[ ] Per-state UI fully spec each pattern
[ ] L1 §6 friction calibration per regulated AR
[ ] Per-pack overlay substantive
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase

```
S3-09_F5_F6_DEEP_UPGRADE_COMPLETE
```

After: load `S3-10_F7_F8.md`.
