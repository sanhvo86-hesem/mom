# S3-08 — F3 Module List + F4 Workspace

```
prompt_id: S3-08    stream: 3    sequence: 8 of 12    effort: ~80 min
```

## Pre-flight reading

```
1. S3-00 stream master
2. V9: F3_MODULE_LIST_SCREENS.md, F4_WORKSPACE_PROJECTIONS.md
3. Cross-refs: F0/F1/F2 (just-upgraded); F5-F12; E2 + E5 + E9;
   per-pack J1-J5; ADR-0009 (tokens); WCAG 2.2 AA
4. Standards: WAI-ARIA 1.2; WCAG 2.2 SC; ICU MessageFormat 2;
   ISA-101 HMI; per pack regulator UI requirements
```

## Deliverable

```
PART_F_FRONTEND_CATALOG/F3_MODULE_LIST_SCREENS.md
PART_F_FRONTEND_CATALOG/F4_WORKSPACE_PROJECTIONS.md
```

## Depth requirements

F3 — Module List spec:
- 14 module catalog × per-pack overlay (≥ 70 module variants)
- Per-module pattern (header / summary tiles / quick links /
  filters / recent activity / pack banner / alerts / AI advisory
  chip)
- Per-role view (operator / supervisor / quality manager /
  domain lead / compliance / QP/PRRC / tenant admin / auditor)
- Per-portal variant
- Per-pack overlay substantive
- Backend bindings (per F9)

F4 — Workspace Projection spec:
- ≥ 30 workspace types catalogued (per Wave 1 18 roots + per
  pack expansion)
- Per-workspace contract (data product per E5 §2.3 / projection
  per E5 §2.1; freshness SLO; per-tab structure; live-data pattern;
  banner state degraded / partial-access / live-mode)
- Per-workspace per-state UI (loading / empty / error / partial /
  degraded / fixture-mode; per ADR-0001)
- Per-workspace search + filter + bulk-select pattern
- Per-workspace pack overlays (per J1..J5)
- Per-workspace SLO target (render p95 < 250ms; freshness < 5s)
- Per-workspace failure modes
- Per-workspace KPIs

## Required substance

F3: ≥ 4,500 words; F4: ≥ 6,500 words

## Acceptance criteria

```
[ ] F3 14 modules × pack overlay (≥ 70 variants)
[ ] F4 ≥ 30 workspace types
[ ] Per-workspace per-state UI fully spec
[ ] Per-pack overlay substantive
[ ] WCAG 2.2 AA per F11 referenced
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase

```
S3-08_F3_F4_DEEP_UPGRADE_COMPLETE
```

After: load `S3-09_F5_F6.md`.
