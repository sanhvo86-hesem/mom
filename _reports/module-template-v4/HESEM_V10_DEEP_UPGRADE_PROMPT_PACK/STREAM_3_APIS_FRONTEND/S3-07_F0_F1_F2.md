# S3-07 — F0 Overview + F1 Shell + F2 Dashboard

```
prompt_id: S3-07    stream: 3    sequence: 7 of 12    effort: ~80 min
```

## Pre-flight reading

```
1. S3-00 stream master
2. V9: F0_PART_F_OVERVIEW.md, F1_SHELL_AND_NAV.md,
   F2_DASHBOARD_LIST_SCREENS.md
3. Cross-refs: F3-F12 (all other patterns); E5 §2.3 (data
   product); ADR-0001 (pre-prod posture); ADR-0004 (forbidden
   files); ADR-0009 (Graphics Authority); per pack J1-J5
4. Standards: WCAG 2.2 AA; WAI-ARIA 1.2; ISA-101 HMI design;
   Material Design / Apple HIG (informational); ICU
   MessageFormat 2; EAA + Section 508
```

## Deliverable

```
PART_F_FRONTEND_CATALOG/F0_PART_F_OVERVIEW.md
PART_F_FRONTEND_CATALOG/F1_SHELL_AND_NAV.md
PART_F_FRONTEND_CATALOG/F2_DASHBOARD_LIST_SCREENS.md
```

## Depth requirements

F0:
- Pattern catalog (SH/DL/ML/WS/AR/AC/Drawer/Wizard) with
  decision tree of when to use each
- Per-pattern checklist (≥ 10 mandatory elements)
- Cross-pattern composition rules
- Per-pack overlay rules
- Per-portal variant rules (internal / auditor / inspector /
  customer / edge gateway / support)
- Pattern retirement governance (per H7)

F1 — full Shell spec:
- Top bar (logo / tenant switcher / global search / notification
  inbox / help / user menu / density toggle / language toggle /
  pre-prod banner)
- Side navigation (per route grammar; permission-driven; per-pack
  toggle; collapse/expand persistent; recent + pinned)
- Footer (pre-prod banner per ADR-0001; version + commit hash;
  support; DPA / privacy; compliance per pack; a11y)
- Banner area (maintenance; tenant freeze; SLO burn; regulator
  window; security event; AI kill-switch)
- Per-portal variant (internal / auditor / inspector / customer
  DPO / edge gateway glove-mode / support)
- Behavior + a11y discipline + i18n + design tokens + responsive
- Failure modes + KPIs

F2 — Dashboard List spec:
- ≥ 20 dashboard catalog (DL-01..DL-20) with full per-dashboard
  contract (KPI tile spec; trend chart spec; anomaly list spec;
  drill-into target; per data product binding per E5 §2.3;
  freshness SLO target; per-role view; per-pack overlay;
  customer-portal variant where applic)
- Live stream pattern (per E5 §2.4)
- Print + audit-pack-export pattern
- Per-pack overlay (DL-19 + DL-20)
- Per-portal variant (auditor scoped; customer per CVLP;
  inspector pre-cleared)
- Failure modes + KPIs

## Required substance

F0: ≥ 4,000 words; F1: ≥ 4,500 words; F2: ≥ 4,500 words

## Acceptance criteria

```
[ ] F0 pattern decision tree + composition rules
[ ] F1 full Shell spec; per-portal variants
[ ] F2 ≥ 20 dashboards full contract; per-pack overlay
[ ] WCAG 2.2 AA per F11 referenced per pattern
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase

```
S3-07_F0_F1_F2_DEEP_UPGRADE_COMPLETE
```

After: load `S3-08_F3_F4.md`.
