# S4-15 — M4 SM Directory + M5 SLO Directory + M6 Risk Register

```
prompt_id: S4-15    stream: 4    sequence: 15 of 16    effort: ~80 min
```

## Pre-flight
S4-00; V9: M4, M5, M6; cross-refs S1-04 (B4 SM-network);
S1-07 (B9 observability); H9 + I3 + I7 + L1+L4

## Deliverable
M4, M5, M6 (3 files)

## Depth requirements

M4 SM Directory:
- 14 core SMs + per-SM owner + tier + workflow + states +
  banned-decisions
- ≥ 30 pack-specific SMs (Pharma + Auto + Aero + MD + Food)
- Hard couplings cascade matrix (every cross-SM cascade)
- Soft couplings (advisory)
- Per-SM cross-references
- For SM-1 (canonical example): full transition table
- Per other SMs: pointer to D-workflow + per Part C

M5 SLO Directory:
- 22 canonical SLOs (per V9 + per V10 expansion)
- Per-SLO definition (SLI / numerator / denominator /
  exclusions / window / budget / alerts / breach behavior)
- Customer-facing SLA mapping per K1 tier
- Error budget policy (green/yellow/red/exhausted)
- SLO governance (add / retire / tighten / loosen)
- SLO ownership matrix (per team primary + alternate
  on-call)

M6 Risk Register (vendor-side):
- ≥ 50 risks (V10 expansion from V9 ~40)
- Categories: Strategic; Architectural; AI-specific;
  Compliance/Regulatory; Operational; Customer/Commercial;
  Financial/Business
- Per-risk: rating; driver; indicator; controls;
  owner; review cadence; status
- Sustained-target risks per pack
- Cross-link to mitigation (per H8 + per L4)
- Risk-review cadence + governance (monthly + quarterly +
  annual + on-event)

## Required substance
M4: ≥ 6,000; M5: ≥ 5,500; M6: ≥ 6,500 words

## Acceptance criteria
```
[ ] M4 14 + ≥ 30 pack-specific SMs;
   hard + soft couplings exhaustive
[ ] M5 22 SLOs each with full definition;
   tier-SLA mapping
[ ] M6 ≥ 50 risks with full per-risk fields;
   ≥ 7 categories
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase
```
S4-15_M4_M5_M6_DEEP_UPGRADE_COMPLETE
```

After: load S4-16_M7_M8_M9.md.
