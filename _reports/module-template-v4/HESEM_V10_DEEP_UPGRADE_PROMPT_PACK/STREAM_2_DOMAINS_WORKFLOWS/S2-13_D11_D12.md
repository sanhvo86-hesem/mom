# S2-13 — D11 Release to Trace + D12 Complaint to Recall

```
prompt_id:   S2-13
stream:      2
sequence:    13 of 14
effort:      ~80 minutes
```

## Pre-flight reading

```
1. S2-00 stream master
2. V9: D11_RELEASE_TO_TRACE.md, D12_COMPLAINT_TO_RECALL.md
3. Cross-refs: C1 + C5 + C7 + C8; M3 + M4 (SM-1, SM-11);
   D10 (parent for regulated); D6 (CAPA cycle from complaint);
   E15 (per-pack integrations); J1-J5; L1 (BD-8 recall)
4. Standards: 21 CFR Part 7 (recall); 21 CFR 803 + 806 (MD);
   EU MDR vigilance Art 87; DSCSA + EU FMD; UDI;
   FSMA §204 + RFR; ICH E2B(R3); per-OEM warranty
```

## Deliverable

```
PART_D_WORKFLOW_CATALOG/D11_RELEASE_TO_TRACE.md
PART_D_WORKFLOW_CATALOG/D12_COMPLAINT_TO_RECALL.md
```

## Depth requirements

D11-specific:
- ≥ 18 trigger catalog (allocation; ship; service; trial;
  sample; emergency; ITAR; recall replacement; per pack)
- Step substance: allocation; pick + pack (with serial /
  UDI / DSCSA capture); mandatory documents; carrier dispatch;
  in-transit; delivery; genealogy update; serialized
  exchange; customer-side handling
- ≥ 18 branches (per pack)
- Per-pack regulator submission integration:
  DSCSA TI/TH/TS (Pharma); EU FMD decommissioning (Pharma);
  UDI to GUDID + EUDAMED (MD); FSMA §204 KDE/CTE (Food);
  FAA Form 8130-3 RTS (Aero); per-OEM 856 ASN (Auto)
- Cold-chain integrity discipline (Pharma + Food)
- ITAR / EAR boundary (Aero)
- ≥ 12 failure modes; ≥ 12 KPIs

D12-specific:
- Complaint cycle (intake → assessment → investigation →
  response → closure)
- Recall classification per pack:
  Pharma 21 CFR 7 (Class I/II/III) per BD-8
  MD vigilance + FSCA per BD-15
  Auto FAA recall per VIN where applic
  Food FSMA recall per BD-27 + RFR (per H1 §3 24h)
  Aero airworthiness directive cascade
- Recall execution console (per F6)
- Recall scope identification (genealogy walk; per E5)
- Mock recall annual cycle (Food per BD; per pack)
- ≥ 18 trigger catalog
- ≥ 15 branches (per pack + per severity)
- Per H1 §3 regulator notification windows enforcement
- ≥ 15 failure modes; ≥ 12 KPIs

## Required substance

D11: ≥ 5,500 words; D12: ≥ 6,500 words

## Acceptance criteria

```
[ ] Full workflow template per S2-00
[ ] Per-pack regulator submission integration substantive
[ ] BD-8 + BD-15 + BD-27 banned-decision spec
[ ] Mock recall + recall execution spec
[ ] ITAR / EAR boundary
[ ] Cold-chain discipline
[ ] AI advisory integration (AI-20 recall scope; AI-26
    sentiment; AI-28 customer reply)
[ ] ≥ 12 KPIs each
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase

```
S2-13_D11_D12_DEEP_UPGRADE_COMPLETE
```

After: load `S2-14_D13_D14.md`.
