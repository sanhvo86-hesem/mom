# S2-09 — D4 Receive to Inspect + D5 Inspect to Disposition

```
prompt_id:   S2-09
stream:      2
sequence:    9 of 14
effort:      ~80 minutes
```

## Pre-flight reading

```
1. S2-00 stream master
2. V9: D4_RECEIVE_TO_INSPECT.md, D5_INSPECT_TO_DISPOSITION.md
3. Cross-refs: C4+C5+C7; M3+M4 (SM-4, SM-5);
   D2 (parent); D6 (NC); J1-J5 packs; L1 (BD-2)
4. Standards: AIAG sampling; ICH Q6 (Pharma identity);
   ISO 10993 (MD biocomp); FSMA Part 1 (FSVP); AS5553/6174
   (counterfeit); MIL-STD-1916; FDA OOS guidance
```

## Deliverable

```
PART_D_WORKFLOW_CATALOG/D4_RECEIVE_TO_INSPECT.md
PART_D_WORKFLOW_CATALOG/D5_INSPECT_TO_DISPOSITION.md
```

## Depth requirements

Apply S2-00 workflow template (sections 1-13).

D4-specific:
- ≥ 20 trigger catalog (per supplier; per pack; per RMA;
  per consignment; per cold-chain; per ITAR)
- SM-4 full transition table
- Step substance: physical receipt (with cold-chain integrity
  check); receipt record; inspection plan selection (per
  AQL); sampling; inspection execution; disposition
- ≥ 15 branches (per pack: counterfeit + DSCSA + FSVP + UDI +
  cold-chain + ITAR; standard; sample; consignment; RMA)
- ≥ 12 edge cases (mis-shipment; cold-chain breach; counterfeit
  detected; identity test fail; calibration just expired)
- Per-pack overlay (counterfeit Aero; FSVP Food; DSCSA Pharma;
  UDI MD; cold-chain Pharma + Food)
- ≥ 12 failure modes
- ≥ 10 KPIs

D5-specific:
- SM-5 full transition table (4 → 6+ states incl MRB)
- BD-2 banned-decision boundary (regulated disposition by
  human only)
- MRB workflow per multi-party signoff
- Concession path with CDOC addendum cross-link
- ≥ 12 branches (accept; reject scrap; RTV; rework in-house;
  rework supplier; concession; partial; quarantine extended;
  per-pack escalate)
- ≥ 12 failure modes
- ≥ 10 KPIs

## Required substance

D4: ≥ 4,500 words; D5: ≥ 4,500 words

## Acceptance criteria

```
[ ] D4 + D5 full workflow template
[ ] Per-pack overlay substantive
[ ] BD-2 banned-decision boundary
[ ] MRB workflow + multi-sig
[ ] Concession addendum spec
[ ] AI advisory integration
[ ] ≥ 10 KPIs each
[ ] Cross-references resolve
[ ] No marketing
[ ] Decision phrase emitted
```

## Decision phrase

```
S2-09_D4_D5_DEEP_UPGRADE_COMPLETE
```

After: load `S2-10_D6_D7.md`.
