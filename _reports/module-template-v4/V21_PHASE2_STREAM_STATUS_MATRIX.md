# V21 Phase 2 Stream Status Matrix

Date: 2026-04-25
Current main HEAD: `5f6376bb`

| stream | report decision | files touched | pass evidence | warnings | blockers | needs repair? | recommended next action |
|---|---|---|---|---|---|---|---|
| CAPA Slice 4 | `CAPA_SLICE4_PASS_WITH_WARNINGS` | `mom/scripts/portal/72-module-template-v4-bridge.js`; `mom/scripts/portal/73-module-template-v4-renderers.js`; `tests/e2e/module-template-v4.spec.ts`; `tests/e2e/module-template-v4-bridge.spec.ts`; `tests/e2e/module-template-v4-axe.spec.ts`; CAPA fixture JSON/pages; CAPA Chromium snapshots | Full Chromium command reported `155 passed`; static JS checks passed; no fixture production load; forbidden portal/source paths untouched | Fixture-backed development/prototype only; does not prove live backend API behavior, production registry promotion, or governed workflow execution; old PHP server on port 8091 had to be killed during validation | None reported as Phase 2 integration blocker | No immediate reimplementation; QA follow-up only | Keep as landed. Carry CAPA live API toggle replication into Phase 3 planning only after cross-browser baseline blocker is resolved. |
| Live API NQCASE | `LIVE_API_TOGGLE_NQCASE_PASS_WITH_WARNINGS` | `mom/scripts/portal/70-module-template-v4-hydration.js`; `tests/e2e/module-template-v4-live-api.spec.ts`; `tests/e2e/package.json`; `tests/fixtures/module-template-v4/pages/authoritative-record-shell-nc-live-mode.html`; `docs/adr/0011-live-api-toggle-mechanism.md`; report file | Toggle remains opt-in; default fixture mode unchanged; GET `/api/v1/nonconformance-cases/{id}` captured; response adapted; error fallback verified; mutation buttons disabled; focused live API spec passed 3/3 | Full Chromium module-template-v4 suite in that stream failed with existing visual drift and a missing live-mode visual baseline at the time | No default-live or mutation blocker reported; visual drift belongs to QA baseline stream | Follow-up hardening only | Keep opt-in only. Replicate toggle pattern to CAPA in Phase 3 planning, not as a Phase 2 repair. |
| Transactional REST C2 | `TRANSACTIONAL_REST_PASS_WITH_WARNINGS` | `mom/api/controllers/OrderController.php`; `mom/api/routes/rest-routes.php`; `mom/api/openapi.yaml`; endpoint catalog/index JSON; `mom/tests/contract/TransactionalRestTest.php`; `mom/tests/contract/TransactionalLegacyRedirectTest.php`; report file | PHP syntax passed; focused contract tests reported `36 tests, 153 assertions`; endpoint catalog JSON verified; canonical plural routes and legacy redirects covered | Local curl smoke returned `400 Bad Request` in local front-controller setup; PHPStan blocked by existing EQMS-controller debt; full PHPUnit exits `255` before normal summary in this environment | No C2-specific focused contract blocker reported | No immediate Phase 2 repair unless broader backend validation is explicitly approved | Keep additive C2 paths. Document broader PHPStan/PHPUnit debt separately; do not create extra backend APIs in this integration review. |
| Cross-browser D4 | `CROSS_BROWSER_FAIL_BLOCK_NEXT` | `tests/e2e/module-template-v4-visual.spec.ts`; Firefox and WebKit visual snapshot files; Playwright config behavior per report | Firefox and WebKit baseline update and verification passed 41/41 each; full matrix had Firefox 113/0/7 and WebKit 113/0/7 | Chromium baselines were not regenerated due prompt prohibition; CI should remain Chromium-only until Chromium drift is repaired | Current main is blocked by existing Chromium visual baseline drift: 8 failed Chromium visual pages in D4 report | Yes, but only after explicit approval | Create Chromium baseline repair plan. If source render is correct, regenerate only approved Chromium snapshots and verify Chromium visual spec. |

## Integration Interpretation

The first three streams are accepted as landed Phase 2 development/prototype work with documented warnings. Cross-browser D4 remains the active Phase 2 blocker because its report explicitly records `CROSS_BROWSER_FAIL_BLOCK_NEXT` and identifies Chromium visual baseline drift.
