openapi: "3.1.2"
info:
  title: HESEM QMS Portal API
  description: |
    REST and legacy action-based API for the HESEM Engineering Quality Management System Portal.

    **Base URL:** `https://qms.hesem.com.vn/01-QMS-Portal/api/`

    ## Authentication

    The API uses **PHP session cookies** for authentication. All protected endpoints
    require a valid `PHPSESSID` cookie obtained via the login flow.

    When MFA is required (TOTP), the login flow is multi-step:
    1. `POST /api/auth/login` with username + password
    2. If `mfa_required: true` is returned, call `POST /api/auth/mfa` with the TOTP code
    3. If `enroll_required: true` is returned (first-time MFA setup), call `POST /api/auth/enroll` with the TOTP code

    ## CSRF Protection

    All state-changing endpoints (POST, PUT, DELETE) require a `X-CSRF-Token` header.
    The token is returned in every auth status / login response as `csrf_token`.

    ## CORS

    Cross-origin requests are handled by the CORS middleware. Preflight `OPTIONS`
    requests are answered automatically.

    ## Rate Limiting

    Login and MFA endpoints are rate-limited per IP and per username (30 attempts / 300 seconds).
    Rate limit state is returned via headers:

    | Header                  | Description                        |
    |-------------------------|------------------------------------|
    | `X-RateLimit-Limit`     | Max requests in window             |
    | `X-RateLimit-Remaining` | Remaining requests                 |
    | `X-RateLimit-Reset`     | Unix timestamp when window resets  |

    ## Legacy Action Interface

    For backward compatibility, all endpoints can also be invoked via
    `POST /api/?action=<action_name>` with a JSON body. Legacy action names are
    documented on each endpoint.

    ## Response Envelope

    All JSON responses follow a standard envelope:

    ```json
    { "ok": true, "server_time": "2025-06-01T12:00:00+00:00", ...data }
    ```

    Error responses:

    ```json
    { "ok": false, "error": "error_code", "detail": "optional message", "server_time": "..." }
    ```
  version: 2.0.0
  contact:
    name: HESEM QMS Team
    url: https://qms.hesem.com.vn
  license:
    name: Proprietary
    url: https://hesem.com.vn

servers:
  - url: https://qms.hesem.com.vn/01-QMS-Portal/api
    description: Production
  - url: http://localhost:8080/01-QMS-Portal/api
    description: Local development

tags:
  - name: Auth
    description: |
      Authentication, MFA verification, enrollment, logout, and session status.
      Xac thuc, MFA, dang nhap, dang xuat.
  - name: Documents
    description: |
      Document lifecycle management -- create, draft, review, approve, reject, versioning, streaming.
      Quan ly tai lieu -- tao moi, ban nhap, duyet, phe duyet, phien ban.
  - name: Forms
    description: |
      Online form listing, schema retrieval, entry submission, and record ID generation.
      Bieu mau truc tuyen -- danh sach, schema, gui du lieu, ma so ban ghi.
  - name: Files
    description: |
      Filesystem operations -- folder scanning, CRUD, document move/rename/delete.
      Quan ly thu muc va tap tin.
  - name: Users
    description: |
      User management and role-based permission control (admin only).
      Quan ly nguoi dung va phan quyen.
  - name: Admin
    description: |
      Administrative operations -- git sync/pull, cache clearing, settings, portal configuration.
      Quan tri he thong -- dong bo git, xoa cache, cau hinh.
  - name: Dictionary
    description: |
      QMS glossary / terminology management.
      Tu dien thuat ngu QMS.
  - name: Foundation
    description: |
      Master-data slices -- organizations, parties, calendars.
      Du lieu nen tang -- to chuc, ben lien quan, lich.
  - name: Governance
    description: |
      Approval workflows, decision tracking, attachments, and timeline.
      Quy trinh phe duyet, theo doi quyet dinh, dinh kem, va dong thoi gian.
  - name: Finance
    description: |
      Financial control objects -- period close, backdate exception, credit memo, debit memo.
      Doi tuong kiem soat tai chinh -- khoa ky, ngoai le lui ngay, credit memo, debit memo.
  - name: Commercial
    description: |
      Commercial demand objects -- customer purchase orders and quote-to-order commitment controls.
      Doi tuong cau thuong mai -- PO khach hang va kiem soat cam ket quote-to-order.
  - name: System Contract
    description: |
      Read-only backend contract registry for frontend and AI tooling: table registry,
      endpoint catalog, workflows, status options, compliance crosswalk and global ERP+MOM audit.
      Hop dong backend read-only de frontend/AI khong phai doan logic he thong.
  - name: Graphics Governance
    description: |
      Backend authority for design config, template registry, component contracts,
      graphics compliance, impact analysis, rollout, rollback, audit and waivers.
      Browser localStorage is cache only; the backend registry/design artifacts are authority.

paths:
  # ═══════════════════════════════════════════════════════════════════════════
  # AUTH
  # ═══════════════════════════════════════════════════════════════════════════

  /api/auth/status:
    get:
      operationId: authStatus
      tags: [Auth]
      summary: Check authentication status
      description: |
        Returns the current session state -- whether the user is logged in,
        whether MFA verification is pending, or whether MFA enrollment is in progress.

        **Legacy action:** `status`, `auth_status`
      security: []
      responses:
        '200':
          description: Session state returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthStatusResponse'
              examples:
                logged_in:
                  summary: Fully authenticated
                  value:
                    ok: true
                    logged_in: true
                    mfa_pending: false
                    enroll_pending: false
                    pending_expires_in: null
                    auth_expired: null
                    user:
                      username: "john.doe"
                      name: "John Doe"
                      role: "admin"
                      dept: "Quality"
                      active: true
                    csrf_token: "abc123def456"
                    server_time: "2025-06-01T08:00:00+00:00"
                    initialized: true
                mfa_pending:
                  summary: MFA verification required
                  value:
                    ok: true
                    logged_in: false
                    mfa_pending: true
                    enroll_pending: false
                    pending_expires_in: 240
                    auth_expired: null
                    user: null
                    csrf_token: "abc123def456"
                    server_time: "2025-06-01T08:00:00+00:00"
                    initialized: true

  /api/auth/login:
    post:
      operationId: authLogin
      tags: [Auth]
      summary: Login with username and password
      description: |
        Authenticate with credentials. If MFA is enabled for the user, the response
        will indicate `mfa_required: true` and you must follow up with `POST /api/auth/mfa`.
        If MFA enrollment is required, `enroll_required: true` is returned with the
        TOTP secret and otpauth URL.

        An optional `code` (OTP) can be provided inline to complete MFA in a single request.

        **Legacy actions:** `auth_login`, `login`

        **Rate limit:** 30 attempts per 300 seconds per IP and per username.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
            examples:
              basic:
                summary: Username + password only
                value:
                  username: "john.doe"
                  password: "MySecureP@ss1"
              with_otp:
                summary: Username + password + inline OTP
                value:
                  username: "john.doe"
                  password: "MySecureP@ss1"
                  code: "123456"
      responses:
        '200':
          description: Login result (may require additional MFA step).
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/LoginSuccessResponse'
                  - $ref: '#/components/schemas/LoginMfaRequiredResponse'
                  - $ref: '#/components/schemas/LoginEnrollRequiredResponse'
              examples:
                success:
                  summary: Login complete (no MFA or inline OTP accepted)
                  value:
                    ok: true
                    logged_in: true
                    user:
                      username: "john.doe"
                      name: "John Doe"
                      role: "admin"
                      dept: "Quality"
                      active: true
                    csrf_token: "abc123def456"
                mfa_required:
                  summary: MFA verification required
                  value:
                    ok: true
                    mfa_required: true
                    enroll_required: false
                    message: "Enter your authenticator code"
                    pending_expires_in: 300
                    csrf_token: "abc123def456"
                enroll_required:
                  summary: First-time MFA enrollment required
                  value:
                    ok: true
                    mfa_required: false
                    enroll_required: true
                    issuer: "HESEM QMS"
                    account: "john.doe"
                    secret: "JBSWY3DPEHPK3PXP"
                    otpauth_url: "otpauth://totp/HESEM%20QMS:john.doe?secret=JBSWY3DPEHPK3PXP&issuer=HESEM+QMS"
                    message: "Enroll MFA in your Authenticator app"
                    pending_expires_in: 300
                    csrf_token: "abc123def456"
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'
        '500':
          $ref: '#/components/responses/ServerError'

  /api/auth/mfa:
    post:
      operationId: authMfaVerify
      tags: [Auth]
      summary: Verify TOTP code during login
      description: |
        Submit the 6-digit TOTP code from the user's authenticator app.
        Must be called after a login response with `mfa_required: true`.

        Alternatively, credentials can be re-supplied in the body (`username` + `password`)
        if the preauth session has expired.

        **Legacy actions:** `auth_mfa_verify`, `mfa_verify`, `verify`

        **Rate limit:** 60 attempts per 300 seconds per IP.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MfaVerifyRequest'
            example:
              code: "123456"
      responses:
        '200':
          description: MFA verified, session fully authenticated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginSuccessResponse'
              example:
                ok: true
                logged_in: true
                user:
                  username: "john.doe"
                  name: "John Doe"
                  role: "admin"
                  dept: "Quality"
                  active: true
                csrf_token: "abc123def456"
        '400':
          description: MFA not enabled for this user.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                ok: false
                error: "mfa_not_enabled"
                server_time: "2025-06-01T08:00:00+00:00"
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/auth/enroll:
    post:
      operationId: authEnrollVerify
      tags: [Auth]
      summary: Verify TOTP code during MFA enrollment
      description: |
        After a login response with `enroll_required: true`, the user scans the QR code
        and submits the first TOTP code to confirm enrollment.

        **Legacy actions:** `auth_enroll_verify`, `enroll_verify`, `enroll`

        **Rate limit:** 60 attempts per 300 seconds per IP.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  description: 6-digit TOTP code from authenticator app.
                  example: "654321"
      responses:
        '200':
          description: MFA enrolled and session authenticated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/LoginSuccessResponse'
                  - type: object
                    properties:
                      message:
                        type: string
                        example: "MFA enabled"
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/auth/logout:
    post:
      operationId: authLogout
      tags: [Auth]
      summary: Logout and destroy session
      description: |
        Destroy the current server-side session. Requires CSRF token.

        **Legacy actions:** `auth_logout`, `logout`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      responses:
        '200':
          description: Session destroyed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    example: true
                  logged_in:
                    type: boolean
                    example: false
        '403':
          $ref: '#/components/responses/Forbidden'

  # ═══════════════════════════════════════════════════════════════════════════
  # DOCUMENTS
  # ═══════════════════════════════════════════════════════════════════════════

  /api/documents:
    get:
      operationId: docsList
      tags: [Documents]
      summary: List documents visible to current user
      description: |
        Returns documents filtered by the current user's role and visibility settings.

        **Legacy action:** `docs_custom_list`
      responses:
        '200':
          description: Document list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      docs:
                        type: array
                        items:
                          $ref: '#/components/schemas/DocumentSummary'
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                docs:
                  - code: "SOP-101"
                    title: "Document and Data Control"
                    cat: "SOP"
                    path: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100/sop-101-document-and-data-control.html"
                    folder: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100"
                    ext: "html"
                    rev: "1.0"
                    status: "approved"
                    owner: "QA/QMS"
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      operationId: docCreate
      tags: [Documents]
      summary: Create a new document
      description: |
        Create a new document with an initial HTML draft. Requires document editor permission.

        **Legacy action:** `doc_create`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocCreateRequest'
            example:
              code: "SOP-999"
              title: "New Procedure"
              cat: "SOP"
              owner: "QA/QMS"
              folder: ""
              revision: "0.0"
      responses:
        '200':
          description: Document created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      code:
                        type: string
                      path:
                        type: string
                      folder:
                        type: string
                      file_name:
                        type: string
                      revision:
                        type: string
                      draft_rel:
                        type: string
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                code: "SOP-999"
                path: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100/sop-999-new-procedure.html"
                folder: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100"
                file_name: "sop-999-new-procedure.html"
                revision: "0.0"
                draft_rel: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100/_Archive/sop-999-new-procedure_V00.00.html"
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Document code already exists.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                ok: false
                error: "code_exists"
                server_time: "2025-06-01T08:00:00+00:00"

  /api/documents/{code}/versions:
    get:
      operationId: docsListVersions
      tags: [Documents]
      summary: List all versions of a document
      description: |
        Returns the version manifest and workflow state for a document.

        **Legacy action:** `doc_versions_list`
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          description: Document code (e.g. SOP-101).
          example: "SOP-101"
        - name: path
          in: query
          required: true
          schema:
            type: string
          description: Relative path to the base document file.
          example: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100/sop-101-document-and-data-control.html"
      responses:
        '200':
          description: Version list and state.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      code:
                        type: string
                      versions:
                        type: array
                        items:
                          $ref: '#/components/schemas/VersionEntry'
                      state:
                        $ref: '#/components/schemas/DocState'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/documents/stream:
    get:
      operationId: docStream
      tags: [Documents]
      summary: Stream / download a document file
      description: |
        Stream a document file for inline viewing or download. Returns the raw file
        with appropriate Content-Type and Content-Disposition headers.

        **Legacy action:** `doc_stream`
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
          description: Relative path to the document file.
          example: "03-Tai-Lieu-Van-Hanh/01-SOPs/01-SOP-100/sop-101-document-and-data-control.html"
      responses:
        '200':
          description: File content streamed.
          content:
            text/html:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          description: File not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/documents/visibility:
    get:
      operationId: docsVisibilityGet
      tags: [Documents]
      summary: Get hidden document codes
      description: |
        Returns the list of document codes that are hidden from the portal.

        **Legacy action:** `docs_visibility_get`
      responses:
        '200':
          description: Hidden document codes.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      hidden:
                        type: array
                        items:
                          type: string
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                hidden: ["DRAFT-001", "OBSOLETE-X"]
        '401':
          $ref: '#/components/responses/Unauthorized'

  # ── Document action endpoints (legacy POST actions) ──

  /?action=doc_save_draft:
    post:
      operationId: docSaveDraft
      tags: [Documents]
      summary: Save a working draft
      description: |
        Save HTML content as a working draft for an existing document.

        **Legacy action:** `doc_save_draft`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, html, path]
              properties:
                code:
                  type: string
                  description: Document code.
                  example: "SOP-101"
                html:
                  type: string
                  description: Full HTML content of the draft.
                path:
                  type: string
                  description: Relative path to the base document file.
      responses:
        '200':
          description: Draft saved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      draft_rel:
                        type: string
                      revision:
                        type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /?action=doc_submit_review:
    post:
      operationId: docSubmitReview
      tags: [Documents]
      summary: Submit document for review
      description: |
        Submit a document for review/approval. Optionally include updated HTML content.

        **Legacy action:** `doc_submit_review`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path]
              properties:
                code:
                  type: string
                html:
                  type: string
                  description: Optional updated HTML content.
                path:
                  type: string
                note:
                  type: string
                  description: Review note.
                update_type:
                  type: string
                  enum: [minor, major]
                  default: minor
      responses:
        '200':
          description: Document submitted for review.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      status:
                        type: string
                        example: "in_review"
                      revision:
                        type: string

  /?action=doc_approve:
    post:
      operationId: docApprove
      tags: [Documents]
      summary: Approve a document
      description: |
        Approve a document that is in review. Publishes the approved version to the live file.
        Requires approver role.

        **Legacy action:** `doc_approve`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path]
              properties:
                code:
                  type: string
                path:
                  type: string
                effective_date:
                  type: string
                  format: date
                  description: Effective date for the approved version.
                note:
                  type: string
                title:
                  type: string
                  description: Optional title update.
      responses:
        '200':
          description: Document approved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      status:
                        type: string
                        example: "approved"
                      revision:
                        type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Document not found.

  /?action=doc_reject:
    post:
      operationId: docReject
      tags: [Documents]
      summary: Reject a document review
      description: |
        Reject a document submission, reverting it to draft status. Requires approver role.

        **Legacy action:** `doc_reject`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path]
              properties:
                code:
                  type: string
                path:
                  type: string
                reason:
                  type: string
                  description: Rejection reason.
      responses:
        '200':
          description: Document rejected, reverted to draft.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      status:
                        type: string
                        example: "draft"
                      revision:
                        type: string

  /?action=doc_update_meta:
    post:
      operationId: docUpdateMeta
      tags: [Documents]
      summary: Update document metadata
      description: |
        Update metadata fields (title, owner, cat, effective_date, delivery_mode) for a document.

        **Legacy action:** `doc_update_meta`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                patch:
                  type: object
                  description: Fields to update.
                  properties:
                    title:
                      type: string
                    owner:
                      type: string
                    cat:
                      type: string
                    effective_date:
                      type: string
                    delivery_mode:
                      type: string
      responses:
        '200':
          description: Metadata updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      updated:
                        type: boolean

  /?action=doc_delete_drafts:
    post:
      operationId: docDeleteDrafts
      tags: [Documents]
      summary: Delete all draft versions (admin)
      description: |
        Delete all draft version files for a document. Admin only.

        **Legacy action:** `doc_delete_drafts`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path]
              properties:
                code:
                  type: string
                path:
                  type: string
      responses:
        '200':
          description: Drafts deleted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      removed:
                        type: integer
                      state:
                        $ref: '#/components/schemas/DocState'

  /?action=doc_delete_version:
    post:
      operationId: docDeleteVersion
      tags: [Documents]
      summary: Delete a specific version entry (admin)
      description: |
        Delete a specific version from the manifest and remove its file. Admin only.

        **Legacy action:** `doc_delete_version`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path, version]
              properties:
                code:
                  type: string
                path:
                  type: string
                version:
                  type: string
                  description: Version string (e.g. "v1.0").
      responses:
        '200':
          description: Version deleted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      deleted:
                        type: boolean
                      state:
                        $ref: '#/components/schemas/DocState'

  /?action=doc_start_new_revision:
    post:
      operationId: docStartNewRevision
      tags: [Documents]
      summary: Start a new revision cycle
      description: |
        Increment the document revision (minor or major) and create a new draft from the live file.

        **Legacy action:** `doc_start_new_revision`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path]
              properties:
                code:
                  type: string
                path:
                  type: string
                update_type:
                  type: string
                  enum: [minor, major]
                  default: minor
      responses:
        '200':
          description: New revision started.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      revision:
                        type: string
                        example: "1.1"
                      state:
                        $ref: '#/components/schemas/DocState'

  /?action=doc_descriptions_get:
    get:
      operationId: docDescriptionsGet
      tags: [Documents]
      summary: Get document descriptions
      description: |
        Returns a map of document codes to their descriptions.

        **Legacy action:** `doc_descriptions_get`
      responses:
        '200':
          description: Descriptions map.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      descriptions:
                        type: object
                        additionalProperties:
                          type: string

  /?action=save_doc_description:
    post:
      operationId: saveDocDescription
      tags: [Documents]
      summary: Save a document description
      description: |
        Update the description text for a single document.

        **Legacy action:** `save_doc_description`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                description:
                  type: string
      responses:
        '200':
          $ref: '#/components/responses/SuccessOnly'

  /?action=admin_docs_visibility_save:
    post:
      operationId: docsVisibilitySave
      tags: [Documents]
      summary: Save document visibility settings (admin)
      description: |
        Set the list of document codes that should be hidden from the portal. Admin only.

        **Legacy action:** `admin_docs_visibility_save`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [hidden]
              properties:
                hidden:
                  type: array
                  items:
                    type: string
                  description: Array of document codes to hide.
      responses:
        '200':
          description: Visibility settings saved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      hidden:
                        type: array
                        items:
                          type: string
        '403':
          $ref: '#/components/responses/Forbidden'

  # ═══════════════════════════════════════════════════════════════════════════
  # FORMS
  # ═══════════════════════════════════════════════════════════════════════════

  /api/forms:
    get:
      operationId: formsList
      tags: [Forms]
      summary: List available online forms
      description: |
        Returns all forms that are marked as online-enabled in the form control registry.

        **Legacy action:** `online_form_list`
      responses:
        '200':
          description: Form list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      forms:
                        type: array
                        items:
                          $ref: '#/components/schemas/FormSummary'
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                forms:
                  - code: "FRM-631"
                    title: "NCR Report Form"
                    status: "RELEASED"
                    rev: "1.0"
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/forms/{code}/schema:
    get:
      operationId: formsGetSchema
      tags: [Forms]
      summary: Get form schema
      description: |
        Returns the full schema/definition for a specific form from the registry.

        **Legacy action:** `online_form_schema`
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          description: Form code (e.g. FRM-631).
          example: "FRM-631"
      responses:
        '200':
          description: Form schema.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      schema:
                        type: object
                        description: Full form schema from registry.
        '404':
          description: Form not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/forms/{code}/entries:
    get:
      operationId: formsGetEntries
      tags: [Forms]
      summary: Get all entries for a form
      description: |
        Returns all submitted entries for a specific form.

        **Legacy action:** `online_form_entries`
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          example: "FRM-631"
      responses:
        '200':
          description: Entry list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      entries:
                        type: array
                        items:
                          $ref: '#/components/schemas/FormEntry'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      operationId: formsSubmitEntry
      tags: [Forms]
      summary: Submit a form entry
      description: |
        Submit a new entry for an online form. The entry data is stored both
        as an individual JSON file and appended to the consolidated entries file.

        **Legacy action:** `online_form_submit`
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
          example: "FRM-631"
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, data]
              properties:
                code:
                  type: string
                  description: Form code (redundant with path param for backward compat).
                data:
                  type: object
                  description: Form field values.
                  additionalProperties: true
            example:
              code: "FRM-631"
              data:
                ncr_number: "NCR-2025-042"
                description: "Surface finish out of spec on Part XYZ"
                severity: "major"
      responses:
        '200':
          description: Entry submitted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      entry_id:
                        type: string
                        description: Generated unique entry ID.
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                entry_id: "a1b2c3d4e5f6g7h8-1717200000"
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/records/registry:
    get:
      operationId: recordIdRegistry
      tags: [Forms]
      summary: Get record ID counter registry
      description: |
        Returns the current state of all record ID counters (type + year -> last number).

        **Legacy action:** `record_id_registry`
      responses:
        '200':
          description: Counter registry.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      registry:
                        type: object
                        additionalProperties:
                          type: integer
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                registry:
                  NCR_2025: 42
                  CAPA_2025: 15

  /api/records/next-id:
    post:
      operationId: recordIdNext
      tags: [Forms]
      summary: Generate and consume the next record ID
      description: |
        Atomically increment the counter and return the formatted record ID.
        Format: `{TYPE}-{YEAR}-{NNN}` (zero-padded number with configurable digit count).

        **Legacy action:** `record_id_next`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [type]
              properties:
                type:
                  type: string
                  description: Record type prefix (e.g. NCR, CAPA, CAR).
                  example: "NCR"
                digits:
                  type: integer
                  minimum: 1
                  maximum: 6
                  default: 3
                  description: Number of zero-padded digits.
            example:
              type: "NCR"
              digits: 3
      responses:
        '200':
          description: Record ID generated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      record_id:
                        type: string
                        example: "NCR-2025-043"
                      number:
                        type: integer
                        example: 43

  /?action=record_id_peek:
    get:
      operationId: recordIdPeek
      tags: [Forms]
      summary: Preview next record ID without consuming
      description: |
        Preview what the next record ID would be, without incrementing the counter.

        **Legacy action:** `record_id_peek`
      parameters:
        - name: type
          in: query
          required: true
          schema:
            type: string
          example: "NCR"
        - name: digits
          in: query
          schema:
            type: integer
            default: 3
      responses:
        '200':
          description: Previewed record ID.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      record_id:
                        type: string
                      number:
                        type: integer
                      peek:
                        type: boolean
                        example: true

  /?action=form_version_stream:
    get:
      operationId: formVersionStream
      tags: [Forms]
      summary: Stream a form version file
      description: |
        Stream a specific form version file for viewing or download.

        **Legacy action:** `form_version_stream`
      parameters:
        - name: path
          in: query
          required: true
          schema:
            type: string
          description: Relative path to the form version file.
      responses:
        '200':
          description: File content streamed.
          content:
            text/html:
              schema:
                type: string
                format: binary
            application/octet-stream:
              schema:
                type: string
                format: binary
        '404':
          description: File not found.

  # ═══════════════════════════════════════════════════════════════════════════
  # FILES & FOLDERS
  # ═══════════════════════════════════════════════════════════════════════════

  /api/folders:
    get:
      operationId: foldersScan
      tags: [Files]
      summary: Scan filesystem and return document listing
      description: |
        Full scan of the QMS document tree. Returns documents and folders.
        Results are cached for 120 seconds.

        **Legacy action:** `scan_folders`
      responses:
        '200':
          description: Document and folder listing.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      docs:
                        type: array
                        items:
                          $ref: '#/components/schemas/DocumentSummary'
                      folders:
                        type: array
                        items:
                          type: string
                          description: Relative folder path.
                      cached:
                        type: boolean
                        description: Whether results came from cache.
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      operationId: foldersCreate
      tags: [Files]
      summary: Create a new folder
      description: |
        Create a folder in the document tree. Requires document editor permission.

        **Legacy action:** `create_folder`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                parent:
                  type: string
                  description: Parent folder relative path (empty for root).
                  default: ""
                name:
                  type: string
                  description: New folder name (max 120 chars, safe characters only).
                  example: "06-SOP-600"
      responses:
        '200':
          description: Folder created.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      path:
                        type: string
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Folder already exists.

  /?action=rename_folder:
    post:
      operationId: foldersRename
      tags: [Files]
      summary: Rename a folder (admin)
      description: |
        Rename a folder in the document tree. Updates all custom docs references. Admin only.

        **Legacy action:** `rename_folder`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [path, new_name]
              properties:
                path:
                  type: string
                  description: Current relative folder path.
                new_name:
                  type: string
                  description: New folder name.
      responses:
        '200':
          description: Folder renamed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      old_path:
                        type: string
                      new_path:
                        type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Folder not found.

  /?action=delete_folder:
    post:
      operationId: foldersDelete
      tags: [Files]
      summary: Delete a folder (admin, soft-delete)
      description: |
        Move a folder to `_Deleted` (soft delete). Admin only.

        **Legacy action:** `delete_folder`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [path]
              properties:
                path:
                  type: string
      responses:
        '200':
          description: Folder deleted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      deleted:
                        type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Folder not found.

  /?action=move_doc:
    post:
      operationId: filesMove
      tags: [Files]
      summary: Move a document to a different folder
      description: |
        Move a document file to a different folder. Updates archive assets and custom doc entries.

        **Legacy action:** `move_doc`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path, dest_folder]
              properties:
                code:
                  type: string
                path:
                  type: string
                  description: Current relative file path.
                dest_folder:
                  type: string
                  description: Destination folder relative path.
      responses:
        '200':
          description: Document moved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      old_path:
                        type: string
                      new_path:
                        type: string

  /?action=delete_doc:
    post:
      operationId: filesDeleteDoc
      tags: [Files]
      summary: Archive (soft-delete) a document
      description: |
        Move a document file to `_Deleted` and remove it from the custom docs registry.

        **Legacy action:** `delete_doc`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  description: Document code.
      responses:
        '200':
          description: Document archived.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      deleted:
                        type: string

  /?action=rename_doc:
    post:
      operationId: filesRenameDoc
      tags: [Files]
      summary: Rename a document file
      description: |
        Rename a document file (preserving extension). Updates archive assets and custom doc entries.

        **Legacy action:** `rename_doc`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code, path, new_name]
              properties:
                code:
                  type: string
                path:
                  type: string
                new_name:
                  type: string
                  description: New filename (extension will be preserved/appended).
      responses:
        '200':
          description: Document renamed.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      old_path:
                        type: string
                      new_path:
                        type: string
        '409':
          description: Name conflict with existing file.

  /?action=folder_descriptions:
    get:
      operationId: folderDescriptions
      tags: [Files]
      summary: Get folder descriptions
      description: |
        Returns descriptions for folders in the document tree.

        **Legacy action:** `folder_descriptions`
      responses:
        '200':
          description: Folder descriptions map.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      descriptions:
                        type: object
                        additionalProperties:
                          type: string

  # ═══════════════════════════════════════════════════════════════════════════
  # USERS
  # ═══════════════════════════════════════════════════════════════════════════

  /api/users:
    get:
      operationId: usersList
      tags: [Users]
      summary: List all users (admin)
      description: |
        Returns all user accounts (sanitized, no password hashes). Admin only.

        **Legacy action:** `admin_users_list`
      responses:
        '200':
          description: User list.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      users:
                        type: array
                        items:
                          $ref: '#/components/schemas/UserPublic'
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                users:
                  - username: "john.doe"
                    name: "John Doe"
                    role: "admin"
                    dept: "Quality"
                    title: "QMS Engineer"
                    active: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

    post:
      operationId: usersUpsert
      tags: [Users]
      summary: Create or update a user (admin)
      description: |
        Create a new user or update an existing one. Admin only.
        When creating, a random password is generated if none is provided.
        Updatable fields: name, role, dept, title, phone, personal_email, cccd, active, password.

        **Legacy action:** `admin_user_upsert`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpsertRequest'
            example:
              username: "jane.smith"
              name: "Jane Smith"
              role: "user"
              dept: "Engineering"
              title: "Process Engineer"
              active: true
              password: "TempP@ss123"
      responses:
        '200':
          description: User created or updated.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      user:
                        $ref: '#/components/schemas/UserPublic'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'

  /?action=admin_user_delete:
    post:
      operationId: usersDelete
      tags: [Users]
      summary: Delete a user (admin)
      description: |
        Delete a user account. Cannot delete self. Admin only.

        **Legacy action:** `admin_user_delete`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username]
              properties:
                username:
                  type: string
      responses:
        '200':
          description: User deleted.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      deleted:
                        type: string
        '400':
          description: Cannot delete self.
        '403':
          $ref: '#/components/responses/Forbidden'

  /?action=admin_user_reset_password:
    post:
      operationId: usersResetPassword
      tags: [Users]
      summary: Reset a user password (admin)
      description: |
        Reset a user's password. If no password is provided, a random 12-character
        password is generated and returned. Admin only.

        **Legacy action:** `admin_user_reset_password`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username]
              properties:
                username:
                  type: string
                password:
                  type: string
                  description: New password (optional; generated if empty).
      responses:
        '200':
          description: Password reset.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      username:
                        type: string
                      temp_password:
                        type: string
                        description: The new password (temporary).
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: User not found.

  /?action=role_perms_get:
    get:
      operationId: rolePermsGet
      tags: [Users]
      summary: Get role permission configuration
      description: |
        Returns the role-based permission map (e.g. which roles can create docs).

        **Legacy action:** `role_perms_get`
      responses:
        '200':
          description: Permission configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      perms:
                        $ref: '#/components/schemas/RolePermissions'

  /?action=admin_role_perms_save:
    post:
      operationId: rolePermsSave
      tags: [Users]
      summary: Save role permission configuration (admin)
      description: |
        Update role-based permissions. Admin only.

        **Legacy action:** `admin_role_perms_save`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [perms]
              properties:
                perms:
                  $ref: '#/components/schemas/RolePermissions'
      responses:
        '200':
          description: Permissions saved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      perms:
                        $ref: '#/components/schemas/RolePermissions'
        '403':
          $ref: '#/components/responses/Forbidden'

  # ═══════════════════════════════════════════════════════════════════════════
  # ADMIN
  # ═══════════════════════════════════════════════════════════════════════════

  /api/admin/git/sync:
    post:
      operationId: adminGitSync
      tags: [Admin]
      summary: Commit and push document changes to git
      description: |
        Stage all document changes, commit with a timestamped message, and push to remote.
        Admin only.

        **Legacy action:** `admin_git_sync`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      responses:
        '200':
          description: Git sync result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      pushed:
                        type: boolean
                      branch:
                        type: string
                      files:
                        type: array
                        items:
                          type: string
                      status:
                        type: array
                        items:
                          type: string
                      status_entries:
                        type: array
                        items:
                          type: object
                          properties:
                            xy:
                              type: string
                            path:
                              type: string
                      message:
                        type: string
                      commit_output:
                        type: string
                      push_output:
                        type: string
                      head_before:
                        type: string
                      head_after:
                        type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Git operation failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/admin/git/pull:
    post:
      operationId: adminGitPull
      tags: [Admin]
      summary: Pull latest changes from remote
      description: |
        Fetch and pull latest changes. If local changes exist, they are pre-synced
        (committed and pushed) before pulling. Admin only.

        **Legacy action:** `admin_git_pull`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      responses:
        '200':
          description: Git pull result.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      pulled:
                        type: boolean
                      branch:
                        type: string
                      message:
                        type: string
                      before_head:
                        type: string
                      after_head:
                        type: string
                      changed_files:
                        type: array
                        items:
                          type: object
                          properties:
                            status:
                              type: string
                            path:
                              type: string
                            old_path:
                              type: string
                      presync:
                        type: object
                        nullable: true
                        description: Pre-sync commit result (if local changes were present).
                      fetch_output:
                        type: string
                      pull_output:
                        type: string
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          description: Git operation failed.

  /api/admin/cache/clear:
    post:
      operationId: adminClearCache
      tags: [Admin]
      summary: Clear site cache
      description: |
        Reset origin caches via Clear-Site-Data header and invalidate server-side scan cache.
        Admin only.

        **Legacy action:** `admin_clear_site_cache`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      responses:
        '200':
          description: Cache cleared.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      message:
                        type: string
                        example: "Origin cache reset requested."
          headers:
            Clear-Site-Data:
              schema:
                type: string
                example: '"cache"'
        '403':
          $ref: '#/components/responses/Forbidden'

  /?action=get_data_settings:
    get:
      operationId: adminGetSettings
      tags: [Admin]
      summary: Get data collection settings
      description: |
        Returns privacy/data collection feature flags.

        **Legacy action:** `get_data_settings`
      responses:
        '200':
          description: Data collection settings.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      settings:
                        $ref: '#/components/schemas/DataCollectionSettings'

  /?action=save_data_settings:
    post:
      operationId: adminSaveSettings
      tags: [Admin]
      summary: Save data collection settings (admin)
      description: |
        Update privacy/data collection feature flags. Admin only.

        **Legacy action:** `save_data_settings`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DataCollectionSettings'
      responses:
        '200':
          description: Settings saved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      settings:
                        $ref: '#/components/schemas/DataCollectionSettings'
        '403':
          $ref: '#/components/responses/Forbidden'

  /?action=admin_portal_display_config_get:
    get:
      operationId: adminPortalConfigGet
      tags: [Admin]
      summary: Get portal display configuration (admin)
      description: |
        Returns the portal display configuration (enabled extensions, categories, etc.). Admin only.

        **Legacy action:** `admin_portal_display_config_get`
      responses:
        '200':
          description: Portal display configuration.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      config:
                        type: object
                        description: Portal display configuration object.
        '403':
          $ref: '#/components/responses/Forbidden'

  /?action=admin_portal_display_config_save:
    post:
      operationId: adminPortalConfigSave
      tags: [Admin]
      summary: Save portal display configuration (admin)
      description: |
        Update the portal display configuration. Admin only.

        **Legacy action:** `admin_portal_display_config_save`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [config]
              properties:
                config:
                  type: object
                  description: Portal display configuration to save.
      responses:
        '200':
          description: Configuration saved.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      config:
                        type: object
        '403':
          $ref: '#/components/responses/Forbidden'

  # ═══════════════════════════════════════════════════════════════════════════
  # DICTIONARY
  # ═══════════════════════════════════════════════════════════════════════════

  /api/dictionary:
    get:
      operationId: dictList
      tags: [Dictionary]
      summary: List all glossary terms
      description: |
        Returns all terms in the QMS glossary / terminology dictionary.

        **Legacy action:** `dict_list`
      responses:
        '200':
          description: Glossary items.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/DictItem'
              example:
                ok: true
                server_time: "2025-06-01T08:00:00+00:00"
                items:
                  - term: "NCR"
                    meaning: "Non-Conformance Report"
                    vi: "Bao cao khong phu hop"
                    def: "A formal record documenting a non-conformance."
                    ctx: "Quality"
                    rec: "ISO 9001:2015 Clause 10.2"
                    cat: "Quality"
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      operationId: dictUpsert
      tags: [Dictionary]
      summary: Create or update a glossary term (admin)
      description: |
        Add a new term or update an existing one. Admin only.

        **Legacy action:** `dict_upsert`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DictUpsertRequest'
            example:
              term: "CAPA"
              originalTerm: "CAPA"
              meaning: "Corrective and Preventive Action"
              vi: "Hanh dong khac phuc va phong ngua"
              def: "Systematic approach to identifying and eliminating root causes."
              ctx: "Quality"
              rec: "ISO 9001:2015 Clause 10.2"
              cat: "Quality"
      responses:
        '200':
          description: Term saved; full list returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/DictItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'

    delete:
      operationId: dictDelete
      tags: [Dictionary]
      summary: Delete a glossary term (admin)
      description: |
        Remove a term from the glossary. Admin only.

        **Legacy action:** `dict_delete`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [term]
              properties:
                term:
                  type: string
                  description: Term to delete (case-insensitive match).
      responses:
        '200':
          description: Term deleted; updated list returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SuccessEnvelope'
                  - type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: '#/components/schemas/DictItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'

  # ═══════════════════════════════════════════════════════════════════════════
  # FOUNDATION
  # ═══════════════════════════════════════════════════════════════════════════

  /api/v1/foundation/organizations:
    get:
      operationId: listFoundationOrganizations
      tags: [Foundation]
      summary: List organizations
      description: |
        Returns a cursor-paginated list of organizations visible to the current user.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
          description: Maximum number of items to return.
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor for forward pagination.
        - name: sort
          in: query
          schema:
            type: string
          description: Sort clause (e.g. "name:asc").
      responses:
        '200':
          description: Organization list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/v1/foundation/parties:
    get:
      operationId: listFoundationParties
      tags: [Foundation]
      summary: List parties
      description: |
        Returns a cursor-paginated list of parties (contacts, suppliers, customers) visible to the current user.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
          description: Maximum number of items to return.
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor for forward pagination.
        - name: sort
          in: query
          schema:
            type: string
          description: Sort clause (e.g. "name:asc").
      responses:
        '200':
          description: Party list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PartyConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/v1/foundation/calendars:
    get:
      operationId: listFoundationCalendars
      tags: [Foundation]
      summary: List calendars
      description: |
        Returns a cursor-paginated list of calendars visible to the current user.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
          description: Maximum number of items to return.
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor for forward pagination.
        - name: sort
          in: query
          schema:
            type: string
          description: Sort clause (e.g. "name:asc").
      responses:
        '200':
          description: Calendar list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalendarConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  # ═══════════════════════════════════════════════════════════════════════════
  # GOVERNANCE
  # ═══════════════════════════════════════════════════════════════════════════

  /api/v1/governance/approval-groups:
    get:
      operationId: listApprovalGroups
      tags: [Governance]
      summary: List approval groups
      description: |
        Returns a cursor-paginated list of approval groups visible to the current user.
      security:
        - sessionCookie: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
          description: Maximum number of items to return.
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor for forward pagination.
        - name: sort
          in: query
          schema:
            type: string
          description: Sort clause (e.g. "created_at:desc").
      responses:
        '200':
          description: Approval group list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalGroupConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/v1/governance/approval-groups/{approvalGroupId}:
    get:
      operationId: getApprovalGroup
      tags: [Governance]
      summary: Get a single approval group
      description: |
        Returns the full detail of an approval group, including current state and participants.
      security:
        - sessionCookie: []
      parameters:
        - name: approvalGroupId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier of the approval group.
      responses:
        '200':
          description: Approval group detail.
          headers:
            ETag:
              schema:
                type: string
              description: Entity tag for conditional requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalGroupEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/governance/approval-groups/{approvalGroupId}:decide:
    post:
      operationId: decideApprovalGroup
      tags: [Governance]
      summary: Submit a decision on an approval group
      description: |
        Submit an approve, reject, or request-changes decision on an approval group.
        Requires the If-Match header with the current ETag to prevent lost updates.

        Decisions are validated through the ApprovalWorkflowAdapter which enforces:
        state validation, self-approval prohibition, and concurrency protection
        before canonical persistence.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: approvalGroupId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier of the approval group.
        - name: If-Match
          in: header
          required: true
          schema:
            type: string
          description: ETag value from the last GET of the approval group.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DecideApprovalGroupRequest'
      responses:
        '200':
          description: Decision recorded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalDecisionResultEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
        '412':
          $ref: '#/components/responses/PreconditionFailed'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '428':
          $ref: '#/components/responses/PreconditionRequired'

  /api/v1/governance/approval-groups/{approvalGroupId}/timeline:
    get:
      operationId: listApprovalGroupTimeline
      tags: [Governance]
      summary: List timeline events for an approval group
      description: |
        Returns a cursor-paginated list of timeline events (decisions, comments, state changes)
        for the specified approval group.
      security:
        - sessionCookie: []
      parameters:
        - name: approvalGroupId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier of the approval group.
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
          description: Maximum number of items to return.
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor for forward pagination.
      responses:
        '200':
          description: Timeline event list.
          headers:
            ETag:
              schema:
                type: string
              description: Entity tag for the timeline state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApprovalTimelineConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/governance/approval-groups/{approvalGroupId}/attachments:
    get:
      operationId: listApprovalGroupAttachments
      tags: [Governance]
      summary: List attachments for an approval group
      description: |
        Returns a cursor-paginated list of attachments associated with the specified approval group.
      security:
        - sessionCookie: []
      parameters:
        - name: approvalGroupId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier of the approval group.
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 25
          description: Maximum number of items to return.
        - name: cursor
          in: query
          schema:
            type: string
          description: Opaque cursor for forward pagination.
      responses:
        '200':
          description: Attachment list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/governance/attachments/{attachmentId}:
    get:
      operationId: getGovernanceAttachment
      tags: [Governance]
      summary: Get a single governance attachment
      description: |
        Returns the detail of a governance attachment by its ID.
      security:
        - sessionCookie: []
      parameters:
        - name: attachmentId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Unique identifier of the attachment.
      responses:
        '200':
          description: Attachment detail.
          headers:
            ETag:
              schema:
                type: string
              description: Entity tag for conditional requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/governance/attachments:
    post:
      operationId: createGovernanceAttachment
      tags: [Governance]
      summary: Upload a new governance attachment
      description: |
        Upload a file as a governance attachment using multipart/form-data.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: The file to upload.
                approvalGroupId:
                  type: string
                  format: uuid
                  description: Optional approval group to associate the attachment with.
                description:
                  type: string
                  description: Optional description of the attachment.
      responses:
        '201':
          description: Attachment created.
          headers:
            ETag:
              schema:
                type: string
              description: Entity tag for the new resource.
            Location:
              schema:
                type: string
                format: uri
              description: URL of the newly created attachment resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachmentEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /api/v1/governance/override-controls:
    get:
      operationId: listOverrideControls
      tags: [Governance]
      summary: List operational override controls
      description: |
        Returns the governed override records used to authorize temporary gate bypasses with typed reasons,
        electronic signature evidence, and expiry windows.
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Override control list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverrideControlConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createOverrideControl
      tags: [Governance]
      summary: Create an operational override control
      description: |
        Creates a typed, signed, and timeboxed override control. Safe retries are supported through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
          description: Optional idempotency key for safe client retries.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateOverrideControlRequest'
      responses:
        '201':
          description: Override control created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverrideControlEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /api/v1/governance/override-controls/{overrideId}:
    get:
      operationId: getOverrideControl
      tags: [Governance]
      summary: Get a single operational override control
      description: |
        Returns the full override record including approval identity, reason code, electronic signature, and expiry.
      security:
        - sessionCookie: []
      parameters:
        - name: overrideId
          in: path
          required: true
          schema:
            type: string
          description: Unique identifier of the override control.
      responses:
        '200':
          description: Override control detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverrideControlEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/governance/override-controls/{overrideId}:transition:
    post:
      operationId: transitionOverrideControl
      tags: [Governance]
      summary: Transition an operational override control
      description: |
        Revokes, expires, or closes an override control with an auditable reason and safe retry support through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: overrideId
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionOverrideControlRequest'
      responses:
        '200':
          description: Override control transitioned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OverrideControlEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/period-closes:
    get:
      operationId: listPeriodCloses
      tags: [Finance]
      summary: List period close controls
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Period close control list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodCloseConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createPeriodClose
      tags: [Finance]
      summary: Close an accounting period
      description: |
        Closes a period for a ledger scope with approval reason and electronic signature evidence.
        Safe retries are supported through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePeriodCloseRequest'
      responses:
        '201':
          description: Period close created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodCloseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/period-closes/{periodCloseId}:
    get:
      operationId: getPeriodClose
      tags: [Finance]
      summary: Get a single period close control
      security:
        - sessionCookie: []
      parameters:
        - name: periodCloseId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Period close detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodCloseEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/finance/period-closes/{periodCloseId}:transition:
    post:
      operationId: transitionPeriodClose
      tags: [Finance]
      summary: Transition a period close control
      description: |
        Reopens or re-closes a governed accounting period with an auditable reason and safe retry support through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: periodCloseId
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionPeriodCloseRequest'
      responses:
        '200':
          description: Period close transitioned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PeriodCloseEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/backdate-exceptions:
    get:
      operationId: listBackdateExceptions
      tags: [Finance]
      summary: List approved backdate exceptions
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Backdate exception list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackdateExceptionConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createBackdateException
      tags: [Finance]
      summary: Create a backdate exception
      description: |
        Creates a signed exception for posting back into a controlled accounting period. Safe retries are supported through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBackdateExceptionRequest'
      responses:
        '201':
          description: Backdate exception created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackdateExceptionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/backdate-exceptions/{backdateExceptionId}:
    get:
      operationId: getBackdateException
      tags: [Finance]
      summary: Get a single backdate exception
      security:
        - sessionCookie: []
      parameters:
        - name: backdateExceptionId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Backdate exception detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackdateExceptionEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/finance/backdate-exceptions/{backdateExceptionId}:transition:
    post:
      operationId: transitionBackdateException
      tags: [Finance]
      summary: Transition a backdate exception
      description: |
        Revokes, expires, or closes a governed backdate exception with an auditable reason and safe retry support through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: backdateExceptionId
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionBackdateExceptionRequest'
      responses:
        '200':
          description: Backdate exception transitioned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackdateExceptionEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/credit-memos:
    get:
      operationId: listCreditMemos
      tags: [Finance]
      summary: List approved credit memos
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Credit memo list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreditMemoConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createCreditMemo
      tags: [Finance]
      summary: Create a credit memo
      description: |
        Creates an approved credit memo record with safe retry support through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMemoRequest'
      responses:
        '201':
          description: Credit memo created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreditMemoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/credit-memos/{creditMemoId}:
    get:
      operationId: getCreditMemo
      tags: [Finance]
      summary: Get a single credit memo
      security:
        - sessionCookie: []
      parameters:
        - name: creditMemoId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Credit memo detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreditMemoEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/finance/debit-memos:
    get:
      operationId: listDebitMemos
      tags: [Finance]
      summary: List approved debit memos
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Debit memo list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebitMemoConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createDebitMemo
      tags: [Finance]
      summary: Create a debit memo
      description: |
        Creates an approved debit memo record with safe retry support through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMemoRequest'
      responses:
        '201':
          description: Debit memo created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebitMemoEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/finance/debit-memos/{debitMemoId}:
    get:
      operationId: getDebitMemo
      tags: [Finance]
      summary: Get a single debit memo
      security:
        - sessionCookie: []
      parameters:
        - name: debitMemoId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Debit memo detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebitMemoEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/commercial/customer-purchase-orders:
    get:
      operationId: listCustomerPurchaseOrders
      tags: [Commercial]
      summary: List customer purchase orders
      description: |
        Returns the canonical commercial-demand objects extracted from customer PO commitments and linked Sales Orders.
      security:
        - sessionCookie: []
      responses:
        '200':
          description: Customer purchase order list.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerPurchaseOrderConnection'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: createCustomerPurchaseOrder
      tags: [Commercial]
      summary: Create a customer purchase order
      description: |
        Captures a customer commercial commitment as a first-class lifecycle owner.
        Safe retries are supported through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCustomerPurchaseOrderRequest'
      responses:
        '201':
          description: Customer purchase order created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerPurchaseOrderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'

  /api/v1/commercial/customer-purchase-orders/{customerPoId}:
    get:
      operationId: getCustomerPurchaseOrder
      tags: [Commercial]
      summary: Get a single customer purchase order
      security:
        - sessionCookie: []
      parameters:
        - name: customerPoId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Customer purchase order detail.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerPurchaseOrderEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/commercial/customer-purchase-orders/{customerPoId}:transition:
    post:
      operationId: transitionCustomerPurchaseOrder
      tags: [Commercial]
      summary: Transition a customer purchase order
      description: |
        Executes a governed customer-PO lifecycle transition such as acknowledge, confirm, cancel, or close.
        Safe retries are supported through `Idempotency-Key`.
      security:
        - sessionCookie: []
          csrfHeader: []
      parameters:
        - name: customerPoId
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TransitionCustomerPurchaseOrderRequest'
      responses:
        '200':
          description: Customer purchase order transitioned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerPurchaseOrderEnvelope'
        '400':
          $ref: '#/components/responses/BadRequestProblem'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'

  # ═══════════════════════════════════════════════════════════════════════════
  # SYSTEM CONTRACT REGISTRY
  # ═══════════════════════════════════════════════════════════════════════════

  /api/system/contracts:
    get:
      operationId: systemContractsGet
      tags: [System Contract]
      summary: Read the complete backend system contract
      description: |
        Returns the read-only system contract for frontend and AI tooling. This
        endpoint is the canonical discovery surface for table registry, endpoint
        catalog, workflow library, status sets, relationship map, runtime access
        policy, authored business-contract indexes and global ERP+MOM capability audit.

        Storage authority remains `database/migrations/*.sql -> database/schema.sql`.
        Runtime generic access is documented separately at `/api/runtime/{domain}/{table}`.

        **Legacy action:** `registry_system_contract`
      responses:
        '200':
          description: System contract returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemContractEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/table-registry:
    get:
      operationId: registryTableRegistryGet
      tags: [System Contract]
      summary: Read the table registry
      description: |
        Returns the table registry used by registry-backed runtime CRUD and Schema Studio.

        **Legacy action:** `registry_table_registry`
      responses:
        '200':
          description: Table registry returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/endpoint-catalog:
    get:
      operationId: registryEndpointCatalogGet
      tags: [System Contract]
      summary: Read the endpoint catalog
      description: |
        Returns the generated endpoint catalog, including registry-backed runtime endpoints.

        **Legacy action:** `registry_endpoint_catalog`
      responses:
        '200':
          description: Endpoint catalog returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/workflow-library:
    get:
      operationId: registryWorkflowLibraryGet
      tags: [System Contract]
      summary: Read the workflow library
      description: |
        Returns workflow state models and transition rules.

        **Legacy action:** `registry_workflow_library`
      responses:
        '200':
          description: Workflow library returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/status-options:
    get:
      operationId: registryStatusOptionsGet
      tags: [System Contract]
      summary: Read status/dropdown option sets
      description: |
        Returns canonical status option sets. Optional query parameter `key`
        narrows the response to one status set.

        **Legacy action:** `registry_status_options`
      parameters:
        - name: key
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: Status options returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/relation-map:
    get:
      operationId: registryRelationMapGet
      tags: [System Contract]
      summary: Read the relationship map
      description: |
        Returns relationship edges used by Schema Studio and frontend navigation.

        **Legacy action:** `registry_relation_map`
      responses:
        '200':
          description: Relation map returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/compliance-crosswalk:
    get:
      operationId: registryComplianceCrosswalkGet
      tags: [System Contract]
      summary: Read the compliance crosswalk
      description: |
        Returns standards and compliance crosswalk metadata for backend coverage review.

        **Legacy action:** `registry_compliance_crosswalk`
      responses:
        '200':
          description: Compliance crosswalk returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/global-capability-audit:
    get:
      operationId: registryGlobalCapabilityAuditGet
      tags: [System Contract]
      summary: Read the global ERP+MOM capability audit
      description: |
        Returns the source-backed audit that maps global ERP, MOM, MES and EQMS
        capability expectations to current backend registry, contracts, workflows,
        endpoint catalog and OpenAPI visibility.

        **Legacy action:** `registry_global_capability_audit`
      responses:
        '200':
          description: Global capability audit returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/registry/manifest:
    get:
      operationId: registryManifestGet
      tags: [System Contract]
      summary: Read the registry manifest
      description: |
        Returns registry asset coverage and publication metadata.

        **Legacy action:** `registry_manifest`
      responses:
        '200':
          description: Registry manifest returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegistryDocumentEnvelope'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/graphics/design-config:
    get:
      operationId: graphicsDesignConfigGet
      tags: [Graphics Governance]
      summary: Read backend-authoritative design config
      description: |
        Returns the governed design configuration with version and ETag.

        **Legacy actions:** `graphics_design_config_get`, `admin_design_config`
      responses:
        '200':
          description: Design config returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DesignConfigResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: graphicsDesignConfigSave
      tags: [Graphics Governance]
      summary: Save backend-authoritative design config
      description: |
        Persists design config on the backend. Requires optimistic concurrency
        using `If-Match`, `expectedVersion`, or the nested `config._meta.version`
        returned by the last authority read.

        **Legacy actions:** `graphics_design_config_save`, `admin_design_config_save`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DesignConfigSaveRequest'
      responses:
        '200':
          description: Design config saved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DesignConfigResponse'
        '412':
          $ref: '#/components/responses/PreconditionFailed'
        '428':
          $ref: '#/components/responses/PreconditionRequired'

  /api/graphics/templates:
    get:
      operationId: graphicsTemplateRegistryGet
      tags: [Graphics Governance]
      summary: Read the template registry collection
      description: |
        Returns the canonical template registry and component contract index.

        **Action aliases:** `graphics_template_registry_get`, `admin_template_registry_get`
      responses:
        '200':
          description: Template registry returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateRegistryCollectionResponse'

  /api/graphics/templates/{templateId}:
    get:
      operationId: graphicsTemplateGet
      tags: [Graphics Governance]
      summary: Read one template by ID or alias
      description: |
        **Action aliases:** `graphics_template_get`, `admin_template_get`
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      responses:
        '200':
          description: Template returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateEntryResponse'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/graphics/templates/draft:
    post:
      operationId: graphicsTemplateDraftSave
      tags: [Graphics Governance]
      summary: Save a governed template draft
      description: |
        Persists a draft under backend governance. Drafts are controlled work
        products, not production authority until publish.

        **Action aliases:** `graphics_template_draft_save`, `admin_template_draft_save`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplateDraftSaveRequest'
      responses:
        '200':
          description: Draft saved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateDraftResponse'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /api/graphics/templates/{templateId}/clone:
    post:
      operationId: graphicsTemplateClone
      tags: [Graphics Governance]
      summary: Clone a template into a governed draft
      description: |
        **Legacy action:** `graphics_template_clone`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplateCloneRequest'
      responses:
        '200':
          description: Draft clone created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateDraftResponse'

  /api/graphics/templates/{templateId}/deprecate:
    post:
      operationId: graphicsTemplateDeprecate
      tags: [Graphics Governance]
      summary: Deprecate or retire a template
      description: |
        Requires migration plan when modules currently use the template.

        **Action aliases:** `graphics_template_deprecate`, `admin_template_deprecate`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplateDeprecateRequest'
      responses:
        '200':
          description: Template deprecated or retired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateEntryResponse'

  /api/graphics/templates/{templateId}/validate:
    post:
      operationId: graphicsTemplateValidate
      tags: [Graphics Governance]
      summary: Validate a draft or registry template
      description: |
        Runs backend validation for unique template IDs, semantic version,
        valid zones, allowed blocks, module references and compatibility rules.

        **Action aliases:** `graphics_template_validate`, `admin_template_validate`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplateValidateRequest'
      responses:
        '200':
          description: Validation result returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateValidationResponse'

  /api/graphics/templates/{templateId}/publish:
    post:
      operationId: graphicsTemplatePublish
      tags: [Graphics Governance]
      summary: Publish an approved template version
      description: |
        Blocks publish when validation fails, impact blockers are unresolved,
        required gate evidence is missing, release evidence is missing, the
        registry version check fails, or `impactAnalysisId` was not recorded by
        the backend impact-analysis endpoint for the current registry state.

        **Action aliases:** `graphics_template_publish`, `admin_template_publish`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/TemplateIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphicsPublishRequest'
      responses:
        '200':
          description: Template published.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsPublishResultResponse'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'

  /api/graphics/templates/{templateId}/modules:
    get:
      operationId: graphicsTemplateModulesGet
      tags: [Graphics Governance]
      summary: List modules using a template
      description: |
        **Action aliases:** `graphics_template_modules`, `admin_template_modules_get`
      parameters:
        - $ref: '#/components/parameters/TemplateIdPath'
      responses:
        '200':
          description: Module usage returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemplateModulesResponse'

  /api/graphics/components/contracts:
    get:
      operationId: graphicsComponentContractsGet
      tags: [Graphics Governance]
      summary: Read component/block contract registry
      description: |
        **Legacy action:** `graphics_component_contract_registry`
      responses:
        '200':
          description: Component contract registry returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ComponentContractRegistryResponse'

  /api/graphics/compliance/modules:
    get:
      operationId: graphicsComplianceMatrixGet
      tags: [Graphics Governance]
      summary: Read module graphics compliance matrix
      description: |
        **Action aliases:** `graphics_compliance_matrix`, `admin_graphics_compliance_get`
      responses:
        '200':
          description: Compliance matrix returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsComplianceMatrixResponse'

  /api/graphics/compliance/non-compliant-modules:
    get:
      operationId: graphicsNonCompliantModulesGet
      tags: [Graphics Governance]
      summary: List non-compliant modules
      description: |
        **Action aliases:** `graphics_non_compliant_modules`, `admin_graphics_non_compliant_get`
      responses:
        '200':
          description: Non-compliant modules returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsNonCompliantModulesResponse'

  /api/graphics/compliance/bridge-alias-debt:
    get:
      operationId: graphicsBridgeAliasDebtGet
      tags: [Graphics Governance]
      summary: Read bridge alias debt
      description: |
        **Legacy action:** `graphics_bridge_alias_debt`
      responses:
        '200':
          description: Bridge alias debt returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsDebtResponse'

  /api/graphics/compliance/private-css-debt:
    get:
      operationId: graphicsPrivateCssDebtGet
      tags: [Graphics Governance]
      summary: Read private CSS debt
      description: |
        **Legacy action:** `graphics_private_css_debt`
      responses:
        '200':
          description: Private CSS debt returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsDebtResponse'

  /api/graphics/compliance/token-adoption-coverage:
    get:
      operationId: graphicsTokenAdoptionCoverageGet
      tags: [Graphics Governance]
      summary: Read token adoption coverage
      description: |
        **Legacy action:** `graphics_token_adoption_coverage`
      responses:
        '200':
          description: Token coverage returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenAdoptionCoverageResponse'

  /api/graphics/compliance/debt:
    get:
      operationId: graphicsComplianceDebtGet
      tags: [Graphics Governance]
      summary: Read aggregate graphics debt
      description: |
        Aggregates bridge alias debt, private CSS debt and token adoption coverage.

        **Legacy actions:** `admin_graphics_debt_get`
      responses:
        '200':
          description: Aggregate graphics debt returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsDebtAggregateResponse'

  /api/graphics/drift:
    get:
      operationId: graphicsDriftGet
      tags: [Graphics Governance]
      summary: Read graphics drift report
      description: |
        **Action aliases:** `graphics_drift_report`, `admin_graphics_drift_get`
      responses:
        '200':
          description: Drift report returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsDriftResponse'

  /api/graphics/release-blockers:
    get:
      operationId: graphicsReleaseBlockersGet
      tags: [Graphics Governance]
      summary: Read graphics release blockers
      description: |
        Returns release-gate blockers derived from module compliance, drift, pending
        impact analysis and active waivers.

        **Legacy actions:** `graphics_release_blockers_get`, `admin_graphics_release_blockers_get`
      responses:
        '200':
          description: Graphics release blockers returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsReleaseBlockersResponse'

  /api/graphics/change-set:
    get:
      operationId: graphicsChangeSetGet
      tags: [Graphics Governance]
      summary: Read current graphics change set model
      description: |
        Groups token/template/component edits with diff, impact, risk, rollout scope,
        and evidence obligations.

        **Action aliases:** `graphics_change_set_get`, `admin_graphics_change_set_get`
      responses:
        '200':
          description: Graphics change set returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsChangeSetResponse'

  /api/graphics/lineage:
    get:
      operationId: graphicsLineageGet
      tags: [Graphics Governance]
      summary: Read module graphics lineage graph
      description: |
        Returns Admin -> backend authority -> shared tokens -> shared components ->
        template -> module linkage proof.

        **Action aliases:** `graphics_lineage_get`, `admin_graphics_lineage_get`
      responses:
        '200':
          description: Graphics lineage graph returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ModuleGraphicsLineageGraphResponse'

  /api/graphics/runtime-beacon:
    get:
      operationId: graphicsRuntimeBeaconGet
      tags: [Graphics Governance]
      summary: Read runtime graphics compliance beacon
      description: |
        Returns runtime linkage beacons by module.

        **Action aliases:** `graphics_runtime_beacon_get`, `admin_graphics_runtime_beacon_get`
      responses:
        '200':
          description: Runtime beacon returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RuntimeGraphicsComplianceBeaconResponse'

  /api/graphics/debt-observatory:
    get:
      operationId: graphicsDebtObservatoryGet
      tags: [Graphics Governance]
      summary: Read visual debt observatory
      description: |
        Groups bridge alias, private CSS, hardcoded style, and uncontrolled shell debt by
        module, domain, team, and route.

        **Action aliases:** `graphics_debt_observatory_get`, `admin_graphics_debt_observatory_get`
      responses:
        '200':
          description: Visual debt observatory returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VisualDebtObservatoryResponse'

  /api/graphics/environment-policy-packs:
    get:
      operationId: graphicsEnvironmentPolicyPacksGet
      tags: [Graphics Governance]
      summary: Read graphics environment policy packs
      description: |
        Returns office, review, admin, shopfloor, kiosk, TV, and night-shift policy packs.

        **Action aliases:** `graphics_environment_policy_packs_get`, `admin_graphics_environment_policy_packs_get`
      responses:
        '200':
          description: Environment policy packs returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsEnvironmentPolicyPacksResponse'

  /api/graphics/release-dashboard:
    get:
      operationId: graphicsReleaseDashboardGet
      tags: [Graphics Governance]
      summary: Read graphics release dashboard
      description: |
        Summarizes blockers, waivers, drift, debt, affected modules, rollout readiness,
        and post-apply verification obligations.

        **Action aliases:** `graphics_release_dashboard_get`, `admin_graphics_release_dashboard_get`
      responses:
        '200':
          description: Graphics release dashboard returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsReleaseDashboardResponse'

  /api/graphics/impact/token:
    post:
      operationId: graphicsTokenImpactAnalyze
      tags: [Graphics Governance]
      summary: Analyze token impact
      description: |
        **Action aliases:** `graphics_impact_token`, `admin_graphics_impact_token`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenImpactRequest'
      responses:
        '200':
          description: Token impact report returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsImpactReportResponse'

  /api/graphics/impact/template:
    post:
      operationId: graphicsTemplateImpactAnalyze
      tags: [Graphics Governance]
      summary: Analyze template impact
      description: |
        **Action aliases:** `graphics_impact_template`, `admin_graphics_impact_template`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TemplateImpactRequest'
      responses:
        '200':
          description: Template impact report returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsImpactReportResponse'

  /api/graphics/impact/component:
    post:
      operationId: graphicsComponentImpactAnalyze
      tags: [Graphics Governance]
      summary: Analyze component impact
      description: |
        **Action aliases:** `graphics_impact_component`, `admin_graphics_impact_component`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ComponentImpactRequest'
      responses:
        '200':
          description: Component impact report returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsImpactReportResponse'

  /api/graphics/rollouts/stage:
    post:
      operationId: graphicsRolloutStage
      tags: [Graphics Governance]
      summary: Stage graphics governance rollout
      description: |
        **Action aliases:** `graphics_rollout_stage`, `admin_graphics_rollout_stage`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphicsRolloutRequest'
      responses:
        '200':
          description: Rollout staged.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsRolloutResponse'

  /api/graphics/rollouts/apply:
    post:
      operationId: graphicsRolloutApply
      tags: [Graphics Governance]
      summary: Apply staged graphics rollout
      description: |
        **Action aliases:** `graphics_rollout_apply`, `admin_graphics_rollout_apply`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphicsRolloutApplyRequest'
      responses:
        '200':
          description: Rollout applied.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsRolloutResponse'

  /api/graphics/rollouts/rollback:
    post:
      operationId: graphicsRolloutRollback
      tags: [Graphics Governance]
      summary: Roll back a graphics rollout
      description: |
        **Action aliases:** `graphics_rollout_rollback`, `admin_graphics_rollout_rollback`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphicsRollbackRequest'
      responses:
        '200':
          description: Rollout rolled back.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsRolloutResponse'

  /api/graphics/audit:
    get:
      operationId: graphicsAuditHistoryGet
      tags: [Graphics Governance]
      summary: Read graphics governance audit history
      description: |
        **Action aliases:** `graphics_audit_history`, `admin_graphics_audit_history_get`
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 200
      responses:
        '200':
          description: Audit history returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsAuditHistoryResponse'

  /api/graphics/waivers:
    post:
      operationId: graphicsWaiverCreate
      tags: [Graphics Governance]
      summary: Create graphics governance waiver
      description: |
        **Action aliases:** `graphics_waiver_create`, `admin_graphics_waiver_create`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphicsWaiver'
      responses:
        '200':
          description: Waiver created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsWaiverResponse'

  /api/graphics/waivers/{waiverId}/approve:
    post:
      operationId: graphicsWaiverApprove
      tags: [Graphics Governance]
      summary: Approve graphics governance waiver
      description: |
        **Action aliases:** `graphics_waiver_approve`, `admin_graphics_waiver_approve`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/WaiverIdPath'
      responses:
        '200':
          description: Waiver approved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsWaiverResponse'

  /api/graphics/waivers/{waiverId}/expire:
    post:
      operationId: graphicsWaiverExpire
      tags: [Graphics Governance]
      summary: Expire graphics governance waiver
      description: |
        **Action aliases:** `graphics_waiver_expire`, `admin_graphics_waiver_expire`
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - $ref: '#/components/parameters/IfMatchHeader'
        - $ref: '#/components/parameters/WaiverIdPath'
      responses:
        '200':
          description: Waiver expired.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsWaiverResponse'

  /api/graphics/waivers/active:
    get:
      operationId: graphicsActiveWaiversGet
      tags: [Graphics Governance]
      summary: List active graphics governance waivers
      description: |
        **Action aliases:** `graphics_waivers_active`, `admin_graphics_waiver_active_get`
      responses:
        '200':
          description: Active waivers returned.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphicsActiveWaiversResponse'

  /api/runtime/{domain}/{table}:
    get:
      operationId: runtimeEntityList
      tags: [System Contract]
      summary: List records from a registry-backed runtime table
      description: |
        Generic registry-backed runtime access. Use `/api/system/contracts` or
        `/api/registry/table-registry` first to discover valid domain/table pairs,
        primary keys, workflow fields and access policy.
      parameters:
        - name: domain
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Runtime records returned.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      operationId: runtimeEntityCreate
      tags: [System Contract]
      summary: Create a registry-backed runtime record
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - name: domain
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Runtime record created.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /api/runtime/{domain}/{table}/{id}:
    get:
      operationId: runtimeEntityDetail
      tags: [System Contract]
      summary: Get one registry-backed runtime record
      parameters:
        - name: domain
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Runtime record returned.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      operationId: runtimeEntityUpdate
      tags: [System Contract]
      summary: Update one registry-backed runtime record
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - name: domain
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Runtime record updated.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      operationId: runtimeEntityDelete
      tags: [System Contract]
      summary: Delete or archive one registry-backed runtime record according to policy
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - name: domain
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Runtime record deleted or archived.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '409':
          $ref: '#/components/responses/Conflict'

  /api/runtime/{domain}/{table}/{id}/transition:
    post:
      operationId: runtimeEntityTransition
      tags: [System Contract]
      summary: Transition one registry-backed runtime record
      description: |
        Executes a governed status transition through the workflow bridge where
        the table has a managed status field.
      parameters:
        - $ref: '#/components/parameters/CsrfHeader'
        - name: domain
          in: path
          required: true
          schema:
            type: string
        - name: table
          in: path
          required: true
          schema:
            type: string
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Runtime record transitioned.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        '409':
          $ref: '#/components/responses/Conflict'

# ═══════════════════════════════════════════════════════════════════════════════
# COMPONENTS
# ═══════════════════════════════════════════════════════════════════════════════

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: PHPSESSID
      description: Session-authenticated portal user.
    csrfHeader:
      type: apiKey
      in: header
      name: X-CSRF-Token
      description: Required together with sessionCookie on every state-changing slice operation.

  parameters:
    CsrfHeader:
      name: X-CSRF-Token
      in: header
      required: true
      schema:
        type: string
      description: CSRF token from the latest auth status or login response.
    IfMatchHeader:
      name: If-Match
      in: header
      required: false
      schema:
        type: string
      description: Current backend authority ETag. State-changing graphics governance endpoints also accept `expectedVersion` in the JSON body.
    TemplateIdPath:
      name: templateId
      in: path
      required: true
      schema:
        type: string
      description: Template ID, canonical ID, legacy code or alias.
    WaiverIdPath:
      name: waiverId
      in: path
      required: true
      schema:
        type: string
      description: Graphics governance waiver ID.

  schemas:
    # ── Envelopes ──

    SuccessEnvelope:
      type: object
      required: [ok, server_time]
      properties:
        ok:
          type: boolean
          const: true
        server_time:
          type: string
          format: date-time
          description: Server UTC timestamp (ISO 8601).

    RegistryDocumentEnvelope:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [data]
          properties:
            data:
              type: object
              additionalProperties: true
              description: Registry document payload.
            count:
              type: integer
              description: Optional record count for the returned registry.

    SystemContractEnvelope:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [data, summary]
          properties:
            summary:
              type: object
              required: [tableCount, endpointCount, workflowCount, contractObjectCount, globalCapabilityCount]
              properties:
                tableCount:
                  type: integer
                endpointCount:
                  type: integer
                workflowCount:
                  type: integer
                statusSetCount:
                  type: integer
                relationEntityCount:
                  type: integer
                contractObjectCount:
                  type: integer
                contractPackageCount:
                  type: integer
                globalCapabilityCount:
                  type: integer
                globalCapabilityBlockingGapCount:
                  type: integer
                systemContractCriticalGapCount:
                  type: integer
            data:
              type: object
              additionalProperties: true
              properties:
                summary:
                  type: object
                  additionalProperties: true
                storage_authority:
                  type: object
                  properties:
                    databaseSchemaSource:
                      type: string
                    tableCount:
                      type: integer
                    schemaName:
                      type: string
                    databaseName:
                      type: string
                system_contract_authority:
                  type: object
                  additionalProperties: true
                  description: DB-derived read-only authority artifacts generated from table-registry, endpoint-catalog, workflow-library, relation-map, and schema authority. This is not the editable Workspace Design Draft.
                table_registry:
                  type: object
                  additionalProperties: true
                endpoint_catalog:
                  type: object
                  additionalProperties: true
                workflow_library:
                  type: object
                  additionalProperties: true
                status_options:
                  type: object
                  additionalProperties: true
                relation_map:
                  type: object
                  additionalProperties: true
                runtime_access_policy:
                  type: object
                  additionalProperties: true
                business_contracts:
                  type: object
                  additionalProperties: true
                global_capability_audit:
                  type: object
                  additionalProperties: true

    ErrorResponse:
      type: object
      required: [ok, error]
      properties:
        ok:
          type: boolean
          const: false
        error:
          type: string
          description: Machine-readable error code.
        detail:
          type: string
          description: Optional human-readable detail message.
        server_time:
          type: string
          format: date-time

    # ── Graphics Governance ──

    DesignConfig:
      type: object
      description: Backend-authoritative design configuration. Browser localStorage may cache it but is not authority.
      additionalProperties: true
      properties:
        _meta:
          type: object
          additionalProperties: true
          properties:
            version:
              type: string
            updatedAt:
              type: string
              format: date-time
            updatedBy:
              type: string
            authority:
              type: string
            localStorageAuthority:
              type: boolean
              const: false

    DesignConfigSaveRequest:
      type: object
      required: [config]
      properties:
        expectedVersion:
          type: string
          description: Current version or ETag from the last GET response.
        config:
          $ref: '#/components/schemas/DesignConfig'

    DesignConfigResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [config, version, etag, authority]
          properties:
            config:
              $ref: '#/components/schemas/DesignConfig'
            data:
              $ref: '#/components/schemas/DesignConfig'
            version:
              type: string
            etag:
              type: string
            authority:
              type: object
              additionalProperties: true

    TemplateRegistryEntry:
      type: object
      required: [templateId, canonicalId, version, status, owner, moduleArchetype, zones, allowedZones, allowedBlocks, allowedBlocksByZone, governedModules, regulatedCompatibility, shopfloorCompatibility, defaultDensity, supportedDensities, themePolicy, responsivePolicy, qaEvidence, updatedBy, updatedAt, evidenceRefs]
      additionalProperties: true
      properties:
        templateId:
          type: string
        canonicalId:
          type: string
        legacyCode:
          type: string
        aliases:
          type: array
          items:
            type: string
        version:
          type: string
          pattern: '^\d+\.\d+\.\d+'
        status:
          type: string
          enum: [approved, draft, deprecated, retired]
        owner:
          type: string
        moduleArchetype:
          type: string
        description:
          type: object
          additionalProperties: true
        zones:
          type: array
          items:
            type: object
            required: [zoneId, type, required]
            additionalProperties: true
            properties:
              zoneId:
                type: string
              type:
                type: string
              required:
                type: boolean
        allowedBlocksByZone:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
        allowedZones:
          type: array
          items:
            type: string
        allowedBlocks:
          oneOf:
            - type: array
              items:
                type: string
            - type: object
              additionalProperties:
                type: array
                items:
                  type: string
        governedModules:
          type: array
          items:
            type: string
        regulatedCompatibility:
          type: string
        shopfloorCompatibility:
          type: string
        updatedBy:
          type: string
        updatedAt:
          type: string
        evidenceRefs:
          type: array
          items:
            type: string
        defaultDensity:
          type: string
          enum: [compact, default, comfortable, shopfloor]
        supportedDensities:
          type: array
          items:
            type: string
            enum: [compact, default, comfortable, shopfloor]
        themePolicy:
          type: object
          additionalProperties: true
        responsivePolicy:
          type: object
          additionalProperties: true
        qaEvidence:
          type: object
          additionalProperties: true
        releaseManifestRefs:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsReleaseManifestRef'
        waiverIds:
          type: array
          items:
            type: string

    TemplateRegistryCollection:
      type: object
      required: [_meta, templates]
      additionalProperties: true
      properties:
        _meta:
          type: object
          additionalProperties: true
        templates:
          type: array
          items:
            $ref: '#/components/schemas/TemplateRegistryEntry'

    TemplateRegistryCollectionResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [registry, templates, version, etag, persistence]
          properties:
            registry:
              $ref: '#/components/schemas/TemplateRegistryCollection'
            templateRegistry:
              $ref: '#/components/schemas/TemplateRegistryCollection'
            templates:
              type: array
              items:
                $ref: '#/components/schemas/TemplateRegistryEntry'
            version:
              type: string
            etag:
              type: string
            persistence:
              type: object
              additionalProperties: true
            componentContractRegistry:
              $ref: '#/components/schemas/ComponentContractRegistry'

    TemplateEntryResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [template]
          properties:
            template:
              $ref: '#/components/schemas/TemplateRegistryEntry'
            modules:
              type: array
              items:
                $ref: '#/components/schemas/ModuleGraphicsComplianceEntry'
            version:
              type: string
            etag:
              type: string

    TemplateDraft:
      allOf:
        - $ref: '#/components/schemas/TemplateRegistryEntry'
        - type: object
          properties:
            _draft:
              type: object
              additionalProperties: true
              properties:
                savedAt:
                  type: string
                  format: date-time
                savedBy:
                  type: string
                baseRegistryVersion:
                  type: string
                baseRegistryEtag:
                  type: string

    TemplateDraftSaveRequest:
      type: object
      properties:
        expectedVersion:
          type: string
        template:
          $ref: '#/components/schemas/TemplateDraft'
      additionalProperties: true

    TemplateCloneRequest:
      type: object
      required: [newTemplateId]
      properties:
        expectedVersion:
          type: string
        newTemplateId:
          type: string
        canonicalId:
          type: string
        legacyCode:
          type: string
        aliases:
          type: array
          items:
            type: string
        version:
          type: string
        owner:
          type: string

    TemplateDeprecateRequest:
      type: object
      properties:
        expectedVersion:
          type: string
        status:
          type: string
          enum: [deprecated, retired]
        reason:
          type: string
        migrationPlan:
          type: string
        deadline:
          type: string
          format: date

    TemplateValidateRequest:
      type: object
      properties:
        templateId:
          type: string
        template:
          $ref: '#/components/schemas/TemplateDraft'

    TemplateValidationResult:
      type: object
      required: [valid, errors, warnings, checkedAt]
      properties:
        valid:
          type: boolean
        templateId:
          type: string
        checkedAt:
          type: string
          format: date-time
        errors:
          type: array
          items:
            type: object
            additionalProperties: true
            properties:
              field:
                type: string
              message:
                type: string
              code:
                type: string
        warnings:
          type: array
          items:
            type: object
            additionalProperties: true

    TemplateValidationResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [validation]
          properties:
            validation:
              $ref: '#/components/schemas/TemplateValidationResult'
            template:
              $ref: '#/components/schemas/TemplateRegistryEntry'

    TemplateDraftResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [draft, validation]
          properties:
            draft:
              $ref: '#/components/schemas/TemplateDraft'
            validation:
              $ref: '#/components/schemas/TemplateValidationResult'
            version:
              type: string
            etag:
              type: string

    ComponentContractRegistry:
      type: object
      required: [_meta, contracts, count]
      properties:
        _meta:
          type: object
          additionalProperties: true
        count:
          type: integer
        contracts:
          type: array
          items:
            type: object
            additionalProperties: true
        contractsByType:
          type: object
          additionalProperties:
            type: object
            additionalProperties: true

    ComponentContractRegistryResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [componentContractRegistry]
          properties:
            componentContractRegistry:
              $ref: '#/components/schemas/ComponentContractRegistry'

    ModuleGraphicsComplianceEntry:
      type: object
      required: [moduleId, templateId, compliant, findings]
      additionalProperties: true
      properties:
        moduleId:
          type: string
        route:
          type: string
        templateId:
          type: string
        templateVersion:
          type: string
        buildPacket:
          type: string
        regulated:
          type: boolean
        shopfloor:
          type: boolean
        screens:
          type: array
          items:
            type: string
        blockFamilies:
          type: array
          items:
            type: string
        gateState:
          type: object
          additionalProperties: true
        findings:
          type: array
          items:
            type: object
            additionalProperties: true
        compliant:
          type: boolean
        linkageStatus:
          type: string
          enum: [full-admin-controlled, bridged-to-shared-tokens, legacy-private-css, blocked]
        templateBindingSource:
          type: string
        sharedTokenCoverage:
          type: number
        sharedComponentCoverage:
          type: number
        bridgeAliasDebt:
          type: integer
        privateCssDebt:
          type: integer
        hardcodedStyleDebt:
          type: integer
        driftStatus:
          type: string
        blockerReason:
          type: string
        consumesHmComponents:
          type: boolean
        consumesSharedTokens:
          type: boolean
        usesPrivateCssShell:
          type: boolean
        reason:
          type: string

    GraphicsComplianceMatrixResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [matrix, summary]
          properties:
            matrix:
              type: array
              items:
                $ref: '#/components/schemas/ModuleGraphicsComplianceEntry'
            summary:
              type: object
              additionalProperties: true
            version:
              type: string
            etag:
              type: string

    GraphicsNonCompliantModulesResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [modules, count]
          properties:
            modules:
              type: array
              items:
                $ref: '#/components/schemas/ModuleGraphicsComplianceEntry'
            count:
              type: integer
            summary:
              type: object
              additionalProperties: true

    GraphicsDebtResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          additionalProperties: true
          properties:
            debt:
              type: array
              items:
                type: object
                additionalProperties: true
            summary:
              type: object
              additionalProperties: true

    GraphicsDebtAggregateResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [debt, summary]
          properties:
            debt:
              type: object
              additionalProperties: true
            summary:
              type: object
              additionalProperties: true

    TokenAdoptionCoverageResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [coverage]
          properties:
            coverage:
              type: object
              additionalProperties: true
            files:
              type: array
              items:
                type: object
                additionalProperties: true

    GraphicsDriftResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [drift, blockers]
          properties:
            drift:
              type: object
              additionalProperties: true
            blockers:
              type: array
              items:
                type: object
                additionalProperties: true

    GraphicsReleaseBlockersResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [blockers, summary]
          properties:
            blockers:
              type: array
              items:
                $ref: '#/components/schemas/GraphicsReleaseBlocker'
            summary:
              type: object
              additionalProperties: true
            evidence:
              $ref: '#/components/schemas/GraphicsReleaseLink'

    GraphicsReleaseBlocker:
      type: object
      additionalProperties: true
      properties:
        blockerId:
          type: string
        scope:
          type: string
          enum: [module, drift, impact-analysis, template, component, token, graphics]
        targetId:
          type: string
        moduleId:
          type: string
        templateId:
          type: string
        severity:
          type: string
          enum: [warning, blocker, waived]
        status:
          type: string
          enum: [active, waived, pending-review]
        reason:
          type: string
        releaseGate:
          type: string
        releaseBlocked:
          type: boolean
        waiverAllowed:
          type: boolean

    GraphicsEvidencePlan:
      type: object
      additionalProperties: true
      properties:
        evidenceType:
          type: string
        required:
          type: boolean
        owner:
          type: string

    GraphicsChangeSet:
      type: object
      required: [changeSetId, status, impact, rolloutScopePlan, evidenceChecklist]
      additionalProperties: true
      properties:
        changeSetId:
          type: string
        status:
          type: string
          enum: [preview-only, impact-recorded, draft-saved, validated, staged, publish-blocked, applied, rolled-back, waived]
        source:
          type: string
        edits:
          type: array
          items:
            type: object
            additionalProperties: true
        diffSummary:
          type: object
          additionalProperties: true
        impact:
          $ref: '#/components/schemas/GraphicsImpactReport'
        risk:
          type: object
          additionalProperties: true
        rolloutScopePlan:
          type: array
          items:
            type: object
            additionalProperties: true
        evidenceChecklist:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsEvidencePlan'

    GraphicsChangeSetResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [changeSet]
          properties:
            changeSet:
              $ref: '#/components/schemas/GraphicsChangeSet'

    ModuleGraphicsLineageGraph:
      type: object
      required: [nodes, edges, summary]
      additionalProperties: true
      properties:
        nodes:
          type: array
          items:
            type: object
            additionalProperties: true
        edges:
          type: array
          items:
            type: object
            additionalProperties: true
        summary:
          type: object
          additionalProperties: true
        authorityRefs:
          type: object
          additionalProperties: true

    ModuleGraphicsLineageGraphResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [lineageGraph]
          properties:
            lineageGraph:
              $ref: '#/components/schemas/ModuleGraphicsLineageGraph'

    RuntimeGraphicsComplianceBeacon:
      type: object
      required: [beacons, summary]
      additionalProperties: true
      properties:
        beacons:
          type: array
          items:
            type: object
            additionalProperties: true
        summary:
          type: object
          additionalProperties: true

    RuntimeGraphicsComplianceBeaconResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [runtimeBeacon]
          properties:
            runtimeBeacon:
              $ref: '#/components/schemas/RuntimeGraphicsComplianceBeacon'

    VisualDebtObservatory:
      type: object
      required: [byModule, summary]
      additionalProperties: true
      properties:
        byModule:
          type: array
          items:
            type: object
            additionalProperties: true
        byDomain:
          type: array
          items:
            type: object
            additionalProperties: true
        byTeam:
          type: array
          items:
            type: object
            additionalProperties: true
        byRoute:
          type: array
          items:
            type: object
            additionalProperties: true
        summary:
          type: object
          additionalProperties: true

    VisualDebtObservatoryResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [observatory]
          properties:
            observatory:
              $ref: '#/components/schemas/VisualDebtObservatory'

    GraphicsEnvironmentPolicyPack:
      type: object
      required: [environment, tokenOverrides, evidenceObligations]
      additionalProperties: true
      properties:
        environment:
          type: string
          enum: [office, review, admin, shopfloor, kiosk, tv, night-shift]
        tokenOverrides:
          type: object
          additionalProperties: true
        evidenceObligations:
          type: array
          items:
            type: string

    GraphicsEnvironmentPolicyPacksResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [policyPacks]
          properties:
            policyPacks:
              type: object
              additionalProperties: true
              properties:
                packs:
                  type: array
                  items:
                    $ref: '#/components/schemas/GraphicsEnvironmentPolicyPack'

    GraphicsReleaseDashboard:
      type: object
      required: [readiness, blockers, complianceSummary, debtSummary, rolloutSummary]
      additionalProperties: true
      properties:
        readiness:
          type: string
          enum: [ready, blocked, unknown]
        blockers:
          type: object
          additionalProperties: true
        waivers:
          type: object
          additionalProperties: true
        complianceSummary:
          type: object
          additionalProperties: true
        debtSummary:
          type: object
          additionalProperties: true
        rolloutSummary:
          type: object
          additionalProperties: true
        postApplyVerification:
          type: object
          additionalProperties: true
        generatedAt:
          type: string
          format: date-time

    GraphicsReleaseDashboardResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [releaseDashboard]
          properties:
            releaseDashboard:
              $ref: '#/components/schemas/GraphicsReleaseDashboard'

    GraphicsImpactReport:
      type: object
      required: [impactId, analysisType, subject, affectedModules, affectedRoutes, affectedScreens, affectedBlockFamilies, regulatedModulesTouched, shopfloorModulesTouched, gatesToRerun, blockers, machineReadable]
      properties:
        impactId:
          type: string
        analysisType:
          type: string
          enum: [token, template, component, rollout, change-set]
        subject:
          type: object
          additionalProperties: true
        affectedModules:
          type: array
          items:
            type: object
            additionalProperties: true
        affectedRoutes:
          type: array
          items:
            type: string
        affectedScreens:
          type: array
          items:
            type: string
        affectedBlockFamilies:
          type: array
          items:
            type: string
        affectedTemplates:
          type: array
          items:
            type: string
        regulatedModulesTouched:
          type: array
          items:
            type: string
        shopfloorModulesTouched:
          type: array
          items:
            type: string
        gatesToRerun:
          type: array
          items:
            type: string
        blockers:
          type: array
          items:
            type: object
            additionalProperties: true
        severityClass:
          type: string
          enum: [low, medium, high, regulated, shopfloor-critical]
        requiredEvidence:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsEvidencePlan'
        rerunPlan:
          type: object
          additionalProperties: true
        rolloutScopes:
          type: array
          items:
            type: object
            additionalProperties: true
        changeSetRef:
          type: string
        machineReadable:
          type: boolean
          const: true
        generatedAt:
          type: string
          format: date-time
        recordedAt:
          type: string
          format: date-time
          description: Present when the backend has stored this impact report as a publish precondition.
        recordedBy:
          type: string
        expiresAt:
          type: string
          format: date-time
          description: Time after which publish must rerun impact analysis.

    GraphicsImpactReportResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - $ref: '#/components/schemas/GraphicsImpactReport'

    TokenImpactRequest:
      type: object
      properties:
        tokenKeys:
          type: array
          items:
            type: string
        tokenGroups:
          type: array
          items:
            type: string

    TemplateImpactRequest:
      type: object
      required: [templateId]
      properties:
        templateId:
          type: string

    ComponentImpactRequest:
      type: object
      properties:
        componentId:
          type: string
        blockType:
          type: string

      GraphicsReleaseManifestRef:
        type: object
        required: [refType, refId]
        properties:
        refType:
          type: string
          enum: [document, dcr, evidence, release_manifest, test_report, approval]
        refId:
          type: string
          uri:
            type: string

      GraphicsReleaseLink:
        type: object
        additionalProperties: true
        properties:
          graphicsAuthorityRefs:
            type: array
            items:
              type: string
          templateRegistryVersion:
            type: string
          templateRegistryChecksum:
            type: string
          complianceMatrixRef:
            type: string
          impactAnalysisRef:
            type: string
          waiversRef:
            type: string
          driftReportGeneratedAt:
            type: string
            format: date-time

    GraphicsPublishRequest:
      type: object
      required: [expectedVersion, impactAnalysisId, gateEvidence, releaseManifestRefs]
      properties:
        expectedVersion:
          type: string
        templateId:
          type: string
        template:
          $ref: '#/components/schemas/TemplateDraft'
        impactAnalysisId:
          type: string
          description: Impact ID returned by a backend impact-analysis endpoint and recorded as pending for the current registry/module state.
        blockersResolved:
          type: boolean
          default: false
        gateEvidence:
          oneOf:
            - type: object
              additionalProperties: true
            - type: array
              items:
                type: object
                additionalProperties: true
        releaseManifestRefs:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsReleaseManifestRef'
        waiverIds:
          type: array
          items:
            type: string

    GraphicsPublishResult:
      type: object
      required: [publishId, template, registryVersion, registryEtag, status]
      properties:
        publishId:
          type: string
        status:
          type: string
          enum: [published]
        template:
          $ref: '#/components/schemas/TemplateRegistryEntry'
        validation:
          $ref: '#/components/schemas/TemplateValidationResult'
        impact:
          $ref: '#/components/schemas/GraphicsImpactReport'
        releaseManifestRefs:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsReleaseManifestRef'
        registryVersion:
          type: string
        registryEtag:
          type: string

    GraphicsPublishResultResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - $ref: '#/components/schemas/GraphicsPublishResult'

    GraphicsRolloutRequest:
      type: object
      required: [expectedVersion]
      properties:
        expectedVersion:
          type: string
        rolloutId:
          type: string
        impactType:
          type: string
          enum: [token, template, component]
        scope:
          type: object
          additionalProperties: true
        scopeMode:
          type: string
          enum: [preview-only, canary-module-group, canary-domain, environment-stage, global-apply]
        templateId:
          type: string
        tokenKeys:
          type: array
          items:
            type: string
        componentId:
          type: string
        releaseManifestRefs:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsReleaseManifestRef'

    GraphicsRolloutApplyRequest:
      type: object
      required: [expectedVersion, rolloutId]
      properties:
        expectedVersion:
          type: string
        rolloutId:
          type: string

    GraphicsRollbackRequest:
      type: object
      required: [expectedVersion, rolloutId]
      properties:
        expectedVersion:
          type: string
        rolloutId:
          type: string
        targetSnapshotId:
          type: string
          description: Optional snapshot ID. Defaults to the rollout rollback snapshot.
        reason:
          type: string

    GraphicsRolloutResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [rollout]
          properties:
            rollout:
              type: object
              additionalProperties: true
            registryVersion:
              type: string
            registryEtag:
              type: string

    GraphicsAuditEvent:
      type: object
      additionalProperties: true
      properties:
        event_type:
          type: string
        aggregate_type:
          type: string
          enum: [graphics_governance]
        aggregate_id:
          type: string
        actor_name:
          type: string
        payload:
          type: object
          additionalProperties: true
        metadata:
          type: object
          additionalProperties: true
        ip_address:
          type: string
        recorded_at:
          type: string
          format: date-time

    GraphicsAuditHistoryResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [events, limit]
          properties:
            events:
              type: array
              items:
                $ref: '#/components/schemas/GraphicsAuditEvent'
            limit:
              type: integer

    GraphicsWaiver:
      type: object
      required: [scope, targetId, reason, expiresAt]
      additionalProperties: true
      properties:
        expectedVersion:
          type: string
        waiverId:
          type: string
          scope:
            type: string
            enum: [template, module, component, token, release_gate, graphics-governance]
          targetId:
            type: string
          reason:
            type: string
          risk:
            type: string
          compensatingControl:
            type: string
          owner:
            type: string
          approver:
            type: string
          riskClass:
            type: string
            enum: [low, medium, high, regulated]
        status:
          type: string
          enum: [draft, approved, expired]
        documentControlRefs:
          type: array
          items:
            type: string
        releaseManifestRefs:
          type: array
          items:
            $ref: '#/components/schemas/GraphicsReleaseManifestRef'
        createdAt:
          type: string
          format: date-time
        createdBy:
          type: string
        approvedAt:
          type: string
          format: date-time
        approvedBy:
          type: string
        expiredAt:
          type: string
          format: date-time
        expiredBy:
          type: string
        expiresAt:
          type: string
          format: date-time

    GraphicsWaiverResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [waiver]
          properties:
            waiver:
              $ref: '#/components/schemas/GraphicsWaiver'
            version:
              type: string
            etag:
              type: string

    GraphicsActiveWaiversResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [waivers]
          properties:
            waivers:
              type: array
              items:
                $ref: '#/components/schemas/GraphicsWaiver'
            version:
              type: string
            etag:
              type: string

    TemplateModulesResponse:
      allOf:
        - $ref: '#/components/schemas/SuccessEnvelope'
        - type: object
          required: [templateId, modules]
          properties:
            templateId:
              type: string
            modules:
              type: array
              items:
                $ref: '#/components/schemas/ModuleGraphicsComplianceEntry'

    # ── Auth ──

    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
          description: Username (case-insensitive).
          example: "john.doe"
        password:
          type: string
          format: password
          description: User password.
        code:
          type: string
          description: Optional 6-digit TOTP code for inline MFA verification.

    MfaVerifyRequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          description: 6-digit TOTP code.
          example: "123456"
        username:
          type: string
          description: Optional fallback -- re-supply credentials if preauth session expired.
        password:
          type: string
          format: password
          description: Optional fallback password.

    AuthStatusResponse:
      type: object
      properties:
        ok:
          type: boolean
        logged_in:
          type: boolean
        mfa_pending:
          type: boolean
        enroll_pending:
          type: boolean
        pending_expires_in:
          type: integer
          nullable: true
          description: Seconds remaining for pending auth (MFA/enroll window).
        auth_expired:
          type: string
          nullable: true
          enum: [mfa, enroll, null]
          description: Which pending auth expired, if any.
        user:
          $ref: '#/components/schemas/UserPublic'
        csrf_token:
          type: string
        server_time:
          type: string
          format: date-time
        initialized:
          type: boolean
          description: Whether the user store has been initialized.

    LoginSuccessResponse:
      type: object
      properties:
        ok:
          type: boolean
          const: true
        logged_in:
          type: boolean
          const: true
        user:
          $ref: '#/components/schemas/UserPublic'
        csrf_token:
          type: string

    LoginMfaRequiredResponse:
      type: object
      properties:
        ok:
          type: boolean
          const: true
        mfa_required:
          type: boolean
          const: true
        enroll_required:
          type: boolean
          const: false
        message:
          type: string
        pending_expires_in:
          type: integer
        csrf_token:
          type: string

    LoginEnrollRequiredResponse:
      type: object
      properties:
        ok:
          type: boolean
          const: true
        mfa_required:
          type: boolean
          const: false
        enroll_required:
          type: boolean
          const: true
        issuer:
          type: string
        account:
          type: string
        secret:
          type: string
          description: Base32-encoded TOTP secret.
        otpauth_url:
          type: string
          format: uri
          description: OTP auth URL for QR code generation.
        message:
          type: string
        pending_expires_in:
          type: integer
        csrf_token:
          type: string

    # ── Users ──

    UserPublic:
      type: object
      description: Sanitized user record (no password hash or MFA secret).
      properties:
        username:
          type: string
        name:
          type: string
        role:
          type: string
          description: "Role key (e.g. admin, user, viewer, editor)."
        dept:
          type: string
          description: Department name.
        title:
          type: string
          description: Job title.
        phone:
          type: string
        personal_email:
          type: string
        cccd:
          type: string
          description: Vietnamese citizen ID.
        active:
          type: boolean
        mfa_enabled:
          type: boolean
        last_login:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    UserUpsertRequest:
      type: object
      required: [username]
      properties:
        username:
          type: string
        name:
          type: string
        role:
          type: string
          default: "user"
        dept:
          type: string
        title:
          type: string
        phone:
          type: string
        personal_email:
          type: string
        cccd:
          type: string
        active:
          type: boolean
          default: true
        password:
          type: string
          format: password
          description: Password (generated if empty on create).

    RolePermissions:
      type: object
      additionalProperties:
        type: object
        properties:
          canCreateDocs:
            type: boolean
      example:
        admin:
          canCreateDocs: true
        editor:
          canCreateDocs: true
        user:
          canCreateDocs: false
        viewer:
          canCreateDocs: false

    # ── Documents ──

    DocumentSummary:
      type: object
      properties:
        code:
          type: string
          description: Document code (e.g. SOP-101).
        title:
          type: string
        cat:
          type: string
          description: Category (SOP, WI, FRM, ANNEX, JD, MAN, POL, etc.).
        path:
          type: string
          description: Relative file path from project root.
        folder:
          type: string
          description: Relative folder path.
        ext:
          type: string
          description: File extension (html, pdf, xlsx, etc.).
        rev:
          type: string
          description: Current revision string (e.g. "1.0").
        status:
          type: string
          enum: [draft, in_review, approved, obsolete]
        owner:
          type: string
          description: Owning department or person.

    DocCreateRequest:
      type: object
      required: [code, title, cat]
      properties:
        code:
          type: string
          description: Unique document code (e.g. SOP-999).
          pattern: '^[A-Z0-9-]{3,40}$'
        title:
          type: string
          description: Document title (ASCII only).
        cat:
          type: string
          description: Category code (SOP, WI, FRM, etc.).
        owner:
          type: string
          description: Owning department (defaults to user's dept).
        folder:
          type: string
          description: Target folder (auto-resolved if empty).
        revision:
          type: string
          description: Initial revision (e.g. "0.0").
          default: "0.0"

    DocState:
      type: object
      description: Document workflow state.
      properties:
        code:
          type: string
        status:
          type: string
          enum: [draft, in_review, approved]
        revision:
          type: string
        has_release:
          type: boolean
        released_revision:
          type: string
        approved_by:
          type: string
        approved_date:
          type: string
          format: date-time
        effective_date:
          type: string
          format: date
        rejectedBy:
          type: string
        rejectedDate:
          type: string
          format: date-time

    VersionEntry:
      type: object
      properties:
        status:
          type: string
          enum: [draft, in_review, approved, rejected]
        version:
          type: string
          description: Version label (e.g. "v1.0").
        date:
          type: string
          description: Human-readable datetime.
        by:
          type: string
          description: Name of the person who performed the action.
        file:
          type: string
          description: Relative path to the version file.
        note:
          type: string

    # ── Forms ──

    FormSummary:
      type: object
      properties:
        code:
          type: string
          description: Form code (e.g. FRM-631).
        title:
          type: string
        status:
          type: string
          description: Control status (e.g. RELEASED).
        rev:
          type: string

    FormEntry:
      type: object
      description: A submitted form entry with dynamic fields.
      properties:
        entry_id:
          type: string
        form_code:
          type: string
        submitted_by:
          type: string
        submitted_name:
          type: string
        submitted_at:
          type: string
          format: date-time
      additionalProperties: true

    # ── Dictionary ──

    DictItem:
      type: object
      properties:
        term:
          type: string
          description: English term.
        meaning:
          type: string
          description: Short meaning / expansion.
        vi:
          type: string
          description: Vietnamese translation.
        def:
          type: string
          description: Full definition.
        ctx:
          type: string
          description: Context / usage domain.
        rec:
          type: string
          description: Reference (ISO clause, standard, etc.).
        cat:
          type: string
          description: Category.
          default: "General"

    DictUpsertRequest:
      type: object
      required: [term, def]
      properties:
        term:
          type: string
        originalTerm:
          type: string
          description: Original term name if renaming (for lookup).
        meaning:
          type: string
        vi:
          type: string
        def:
          type: string
        ctx:
          type: string
        rec:
          type: string
        cat:
          type: string
          default: "General"

    # ── Admin ──

    DataCollectionSettings:
      type: object
      properties:
        collect_gps:
          type: boolean
          default: true
        collect_ip:
          type: boolean
          default: true
        collect_device:
          type: boolean
          default: true
        collect_navigation:
          type: boolean
          default: true
        collect_connection:
          type: boolean
          default: true
        require_consent:
          type: boolean
          default: true

    # ── Pagination ──

    CursorPageInfo:
      type: object
      description: Cursor-based pagination metadata.
      required: [limit, hasNextPage, hasPreviousPage]
      properties:
        limit:
          type: integer
          description: Maximum items per page.
        hasNextPage:
          type: boolean
          description: Whether more items exist after the last item in this page.
        hasPreviousPage:
          type: boolean
          description: Whether more items exist before the first item in this page.
        startCursor:
          type: string
          nullable: true
          description: Cursor pointing to the first item in this page.
        endCursor:
          type: string
          nullable: true
          description: Cursor pointing to the last item in this page.
        sort:
          type: array
          items:
            $ref: '#/components/schemas/SortClause'
          description: Active sort order applied to the result set.

    SortClause:
      type: object
      required: [field, direction]
      properties:
        field:
          type: string
          description: Field name being sorted on.
        direction:
          type: string
          enum: [asc, desc]
          description: Sort direction.

    # ── RFC 9457 Problem Detail ──

    ProblemDetail:
      type: object
      description: RFC 9457 Problem Details for HTTP APIs.
      required: [type, title, status]
      properties:
        type:
          type: string
          format: uri
          description: URI reference identifying the problem type.
          example: "https://qms.hesem.com.vn/problems/validation-error"
        title:
          type: string
          description: Short human-readable summary of the problem type.
          example: "Validation Error"
        status:
          type: integer
          description: HTTP status code.
          example: 422
        detail:
          type: string
          description: Human-readable explanation specific to this occurrence.
        instance:
          type: string
          format: uri
          description: URI reference identifying the specific occurrence.
        code:
          type: string
          description: Machine-readable error code.
        trace_id:
          type: string
          description: Distributed trace identifier for correlation.
        current_etag:
          type: string
          description: Current ETag value when a concurrency conflict occurs.
        errors:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
                description: JSON pointer or field name.
              message:
                type: string
                description: Validation error message.
              code:
                type: string
                description: Machine-readable validation error code.
          description: Detailed validation errors for 422 responses.

    # ── Foundation Items ──

    OrganizationItem:
      type: object
      description: A single organization record.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        code:
          type: string
        type:
          type: string
          description: Organization type (e.g. internal, supplier, customer).
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PartyItem:
      type: object
      description: A single party record (contact, supplier, customer).
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        role:
          type: string
        organizationId:
          type: string
          format: uuid
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CalendarItem:
      type: object
      description: A single calendar record.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        timezone:
          type: string
          description: IANA timezone identifier.
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ── Governance Items ──

    ApprovalGroupItem:
      type: object
      description: Summary of an approval group for list views.
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        status:
          type: string
          description: Current workflow status.
        createdBy:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ApprovalGroupDetail:
      type: object
      description: Full detail of an approval group.
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        status:
          type: string
          description: Current workflow status.
        participants:
          type: array
          items:
            type: object
            properties:
              userId:
                type: string
              name:
                type: string
              role:
                type: string
              decision:
                type: string
                nullable: true
              decidedAt:
                type: string
                format: date-time
                nullable: true
        createdBy:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ApprovalDecisionResult:
      type: object
      description: Result of a decision action on an approval group.
      properties:
        approvalGroupId:
          type: string
          format: uuid
        decisionCode:
          type: string
          enum: [approve, reject, request_changes]
        newStatus:
          type: string
          description: Updated status after the decision.
        decidedBy:
          type: string
        decidedAt:
          type: string
          format: date-time

    TimelineEvent:
      type: object
      description: A single timeline event in an approval group.
      properties:
        id:
          type: string
          format: uuid
        eventType:
          type: string
          description: Type of event (decision, comment, status_change, attachment_added).
        actor:
          type: string
          description: Username or ID of the person who performed the action.
        summary:
          type: string
          description: Human-readable summary of the event.
        detail:
          type: object
          additionalProperties: true
          description: Event-specific payload.
        occurredAt:
          type: string
          format: date-time

    AttachmentItem:
      type: object
      description: Summary of an attachment for list views.
      properties:
        id:
          type: string
          format: uuid
        filename:
          type: string
        contentType:
          type: string
        sizeBytes:
          type: integer
          format: int64
        uploadedBy:
          type: string
        uploadedAt:
          type: string
          format: date-time

    AttachmentDetail:
      type: object
      description: Full detail of an attachment.
      properties:
        id:
          type: string
          format: uuid
        filename:
          type: string
        contentType:
          type: string
        sizeBytes:
          type: integer
          format: int64
        description:
          type: string
        approvalGroupId:
          type: string
          format: uuid
          nullable: true
        downloadUrl:
          type: string
          format: uri
        uploadedBy:
          type: string
        uploadedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ── Connection Wrappers (cursor-paginated lists) ──

    OrganizationConnection:
      type: object
      required: [data, pageInfo]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationItem'
        pageInfo:
          $ref: '#/components/schemas/CursorPageInfo'

    PartyConnection:
      type: object
      required: [data, pageInfo]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/PartyItem'
        pageInfo:
          $ref: '#/components/schemas/CursorPageInfo'

    CalendarConnection:
      type: object
      required: [data, pageInfo]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/CalendarItem'
        pageInfo:
          $ref: '#/components/schemas/CursorPageInfo'

    ApprovalGroupConnection:
      type: object
      required: [data, pageInfo]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/ApprovalGroupItem'
        pageInfo:
          $ref: '#/components/schemas/CursorPageInfo'

    ApprovalTimelineConnection:
      type: object
      required: [data, pageInfo]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TimelineEvent'
        pageInfo:
          $ref: '#/components/schemas/CursorPageInfo'

    AttachmentConnection:
      type: object
      required: [data, pageInfo]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AttachmentItem'
        pageInfo:
          $ref: '#/components/schemas/CursorPageInfo'

    # ── Envelope Wrappers (single-object detail) ──

    ApprovalGroupEnvelope:
      type: object
      required: [data]
      properties:
        data:
          $ref: '#/components/schemas/ApprovalGroupDetail'

    ApprovalDecisionResultEnvelope:
      type: object
      required: [data]
      properties:
        data:
          $ref: '#/components/schemas/ApprovalDecisionResult'

    AttachmentEnvelope:
      type: object
      required: [data]
      properties:
        data:
          $ref: '#/components/schemas/AttachmentDetail'

    OverrideControlItem:
      type: object
      required: [override_id, override_type, override_status, subject_type, subject_id, control_code, reason_code, approved_by, approved_at, effective_from, expires_at]
      properties:
        override_id:
          type: string
        override_type:
          type: string
        override_status:
          type: string
        subject_type:
          type: string
        subject_id:
          type: string
        control_family:
          type: string
        control_code:
          type: string
        reason_code:
          type: string
        reason_label:
          type: string
        reason_text:
          type: string
        approved_by:
          type: string
        approved_role:
          type: string
        approved_at:
          type: string
          format: date-time
        effective_from:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        approval_reference:
          type: [string, "null"]
        follow_up_status:
          type: string
        e_signature:
          type: object
          additionalProperties: true

    OverrideControlConnection:
      type: object
      required: [override_controls]
      properties:
        override_controls:
          type: array
          items:
            $ref: '#/components/schemas/OverrideControlItem'

    OverrideControlEnvelope:
      type: object
      required: [override_control]
      properties:
        override_control:
          $ref: '#/components/schemas/OverrideControlItem'

    CreateOverrideControlRequest:
      type: object
      required: [override_type, subject_type, subject_id, control_family, control_code, reason_code, reason_text, approved_by, approved_role]
      properties:
        override_type:
          type: string
        subject_type:
          type: string
        subject_id:
          type: string
        control_family:
          type: string
        control_code:
          type: string
        reason_code:
          type: string
        reason_text:
          type: string
        approved_by:
          type: string
        approved_role:
          type: string
        requested_by:
          type: string
        requested_role:
          type: string
        approval_reference:
          type: string
        effective_from:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        source_context:
          type: object
          additionalProperties: true

    TransitionOverrideControlRequest:
      type: object
      required: [transition, reason]
      properties:
        transition:
          type: string
          enum: [revoke, expire, close]
        reason:
          type: string
        follow_up_status:
          type: string

    PeriodCloseItem:
      type: object
      required: [period_close_id, period_code, ledger_scope, close_status, closed_by, closed_at, reason]
      properties:
        period_close_id:
          type: string
        period_code:
          type: string
          pattern: '^\\d{4}-(0[1-9]|1[0-2])$'
        ledger_scope:
          type: string
          enum: [AP, AR, PLANT]
        close_status:
          type: string
        closed_by:
          type: string
        closed_at:
          type: string
          format: date-time
        reason:
          type: string
        e_signature:
          type: object
          additionalProperties: true

    PeriodCloseConnection:
      type: object
      required: [period_closes]
      properties:
        period_closes:
          type: array
          items:
            $ref: '#/components/schemas/PeriodCloseItem'

    PeriodCloseEnvelope:
      type: object
      required: [period_close]
      properties:
        period_close:
          $ref: '#/components/schemas/PeriodCloseItem'

    CreatePeriodCloseRequest:
      type: object
      required: [period_code, ledger_scope, reason]
      properties:
        period_code:
          type: string
          pattern: '^\\d{4}-(0[1-9]|1[0-2])$'
        ledger_scope:
          type: string
          enum: [AP, AR, PLANT]
        reason:
          type: string

    TransitionPeriodCloseRequest:
      type: object
      required: [transition, reason]
      properties:
        transition:
          type: string
          enum: [reopen, close]
        reason:
          type: string

    BackdateExceptionItem:
      type: object
      required: [backdate_exception_id, exception_status, ledger_scope, subject_type, subject_ref, requested_posting_date, approved_by, approved_at, expires_at]
      properties:
        backdate_exception_id:
          type: string
        exception_status:
          type: string
        ledger_scope:
          type: string
          enum: [AP, AR, PLANT, EXECUTION]
        subject_type:
          type: string
        subject_ref:
          type: string
        reason_code:
          type: string
        reason:
          type: string
        approval_reference:
          type: string
        original_event_at:
          type: string
          format: date-time
        requested_posting_date:
          type: string
          format: date
        approved_period_code:
          type: string
        approved_by:
          type: string
        approved_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
        e_signature:
          type: object
          additionalProperties: true

    BackdateExceptionConnection:
      type: object
      required: [backdate_exceptions]
      properties:
        backdate_exceptions:
          type: array
          items:
            $ref: '#/components/schemas/BackdateExceptionItem'

    BackdateExceptionEnvelope:
      type: object
      required: [backdate_exception]
      properties:
        backdate_exception:
          $ref: '#/components/schemas/BackdateExceptionItem'

    CreateBackdateExceptionRequest:
      type: object
      required: [ledger_scope, subject_type, subject_ref, reason, approval_reference, requested_posting_date, original_event_at, expires_at]
      properties:
        ledger_scope:
          type: string
          enum: [AP, AR, PLANT, EXECUTION]
        subject_type:
          type: string
        subject_ref:
          type: string
        reason_code:
          type: string
        reason:
          type: string
        approval_reference:
          type: string
        requested_posting_date:
          type: string
          format: date
        original_event_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time

    TransitionBackdateExceptionRequest:
      type: object
      required: [transition, reason]
      properties:
        transition:
          type: string
          enum: [revoke, expire, close]
        reason:
          type: string

    MemoItem:
      type: object
      required: [memo_type, memo_status, invoice_scope, original_invoice_ref, amount, currency_code, approved_by, approved_at]
      properties:
        memo_type:
          type: string
          enum: [credit, debit]
        memo_status:
          type: string
        invoice_scope:
          type: string
          enum: [AP, AR]
        original_invoice_ref:
          type: string
        reason_code:
          type: string
        reason:
          type: string
        amount:
          type: number
        currency_code:
          type: string
        approved_by:
          type: string
        approved_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreditMemoItem:
      allOf:
        - $ref: '#/components/schemas/MemoItem'
        - type: object
          required: [credit_memo_id]
          properties:
            credit_memo_id:
              type: string

    DebitMemoItem:
      allOf:
        - $ref: '#/components/schemas/MemoItem'
        - type: object
          required: [debit_memo_id]
          properties:
            debit_memo_id:
              type: string

    CreditMemoConnection:
      type: object
      required: [credit_memos]
      properties:
        credit_memos:
          type: array
          items:
            $ref: '#/components/schemas/CreditMemoItem'

    DebitMemoConnection:
      type: object
      required: [debit_memos]
      properties:
        debit_memos:
          type: array
          items:
            $ref: '#/components/schemas/DebitMemoItem'

    CreditMemoEnvelope:
      type: object
      required: [credit_memo]
      properties:
        credit_memo:
          $ref: '#/components/schemas/CreditMemoItem'

    DebitMemoEnvelope:
      type: object
      required: [debit_memo]
      properties:
        debit_memo:
          $ref: '#/components/schemas/DebitMemoItem'

    CreateMemoRequest:
      type: object
      required: [invoice_scope, original_invoice_ref, reason, amount]
      properties:
        invoice_scope:
          type: string
          enum: [AP, AR]
        original_invoice_ref:
          type: string
        reason_code:
          type: string
        reason:
          type: string
        amount:
          type: number
          exclusiveMinimum: 0
        currency_code:
          type: string
          default: VND

    CustomerPurchaseOrderLine:
      type: object
      properties:
        customer_po_line_id:
          type: string
        line_number:
          type: integer
        customer_item_ref:
          type: string
        item_id:
          type: string
        part_number:
          type: string
        description:
          type: string
        qty:
          type: number
        uom:
          type: string
        unit_price:
          type: number
        line_total:
          type: number
        requested_date:
          type: string
          format: date
        promise_date:
          type: string
          format: date

    CustomerPurchaseOrderItem:
      type: object
      required: [customer_po_id, customer_po_number, po_status, customer_id, received_at]
      properties:
        customer_po_id:
          type: string
        customer_po_number:
          type: string
        po_status:
          type: string
          enum: [received, acknowledged, confirmed, closed, cancelled]
        customer_id:
          type: string
        customer_name:
          type: string
        customer_site_id:
          type: string
        ship_to_site_id:
          type: string
        quote_id:
          type: string
        received_at:
          type: string
          format: date-time
        requested_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
        currency_code:
          type: string
        incoterm_code:
          type: string
        payment_term_code:
          type: string
        customer_reference:
          type: string
        source:
          type: string
        source_system:
          type: string
        source_record_id:
          type: string
        notes:
          type: string
        line_count:
          type: integer
        total_qty:
          type: number
        total_value:
          type: number
        lines:
          type: array
          items:
            $ref: '#/components/schemas/CustomerPurchaseOrderLine'
        sales_order_refs:
          type: array
          items:
            type: object
            additionalProperties: true
        status_history:
          type: array
          items:
            type: object
            additionalProperties: true
        created_at:
          type: string
          format: date-time
        created_by:
          type: string
        updated_at:
          type: string
          format: date-time
        updated_by:
          type: string

    CustomerPurchaseOrderConnection:
      type: object
      required: [customer_purchase_orders]
      properties:
        customer_purchase_orders:
          type: array
          items:
            $ref: '#/components/schemas/CustomerPurchaseOrderItem'

    CustomerPurchaseOrderEnvelope:
      type: object
      required: [customer_purchase_order]
      properties:
        customer_purchase_order:
          $ref: '#/components/schemas/CustomerPurchaseOrderItem'

    CreateCustomerPurchaseOrderRequest:
      type: object
      required: [customer_id, customer_po_number]
      properties:
        customer_id:
          type: string
        customer_name:
          type: string
        customer_site_id:
          type: string
        ship_to_site_id:
          type: string
        customer_po_number:
          type: string
        received_at:
          type: string
          format: date-time
        requested_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
        quote_id:
          type: string
        currency_code:
          type: string
        incoterm_code:
          type: string
        payment_term_code:
          type: string
        customer_reference:
          type: string
        source:
          type: string
        source_system:
          type: string
        source_record_id:
          type: string
        notes:
          type: string
        lines:
          type: array
          items:
            $ref: '#/components/schemas/CustomerPurchaseOrderLine'

    TransitionCustomerPurchaseOrderRequest:
      type: object
      required: [transition]
      properties:
        transition:
          type: string
          enum: [acknowledge, confirm, cancel, close]
        reason:
          type: string
        so_number:
          type: string
        sales_order_status:
          type: string

    # ── Governance Requests ──

    DecideApprovalGroupRequest:
      type: object
      required: [decisionCode]
      properties:
        decisionCode:
          type: string
          enum: [approve, reject, request_changes]
          description: The decision to submit.
        commentText:
          type: string
          description: Optional comment accompanying the decision.
        reasonCode:
          type: string
          description: Optional machine-readable reason code.
        electronicSignatureId:
          type: string
          format: uuid
          description: Optional electronic signature reference for regulatory traceability.

  responses:
    SuccessOnly:
      description: Generic success.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SuccessEnvelope'

    BadRequest:
      description: Bad request -- missing or invalid parameters.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            ok: false
            error: "missing_code"
            server_time: "2025-06-01T08:00:00+00:00"

    Unauthorized:
      description: Not authenticated or invalid credentials.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            ok: false
            error: "unauthorized"
            server_time: "2025-06-01T08:00:00+00:00"

    Forbidden:
      description: Insufficient permissions (not admin or lacking role).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            ok: false
            error: "forbidden"
            server_time: "2025-06-01T08:00:00+00:00"

    RateLimited:
      description: Too many requests.
      headers:
        X-RateLimit-Limit:
          schema:
            type: integer
        X-RateLimit-Remaining:
          schema:
            type: integer
        X-RateLimit-Reset:
          schema:
            type: integer
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            ok: false
            error: "rate_limited"
            server_time: "2025-06-01T08:00:00+00:00"

    ServerError:
      description: Internal server error.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            ok: false
            error: "server_error"
            server_time: "2025-06-01T08:00:00+00:00"

    # ── RFC 9457 ProblemDetail error responses ──

    BadRequestProblem:
      description: Bad request -- the request body or parameters are invalid.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    UnauthorizedProblem:
      description: Authentication required or session expired.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    ForbiddenProblem:
      description: Insufficient permissions for this operation.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    NotFound:
      description: The requested resource was not found.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    Conflict:
      description: Conflict -- the resource state has changed since last read.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    PreconditionFailed:
      description: Precondition failed -- the If-Match ETag does not match the current resource version.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    UnsupportedMediaType:
      description: Unsupported media type -- the request Content-Type is not accepted.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    UnprocessableEntity:
      description: Unprocessable entity -- the request is syntactically valid but semantically incorrect.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

    PreconditionRequired:
      description: Precondition required -- a conditional header (e.g. If-Match) is mandatory for this request.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

security:
  - sessionCookie: []
