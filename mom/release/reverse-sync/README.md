# Reverse-Sync Intake

Reverse-sync intake records quarantine external changes before they can become
controlled source, runtime configuration, records, or publication receipts.

Direct edits in SharePoint, exported reports, vendor packages, and break-glass
changes must be classified here before acceptance or rejection.
