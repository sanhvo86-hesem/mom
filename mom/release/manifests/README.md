# Release Manifests

Controlled release manifests live here after review and approval. Builder output,
smoke logs, screenshots, and temporary bundles do not belong in this lane.

Each manifest must correspond to a `release_manifests` row and must include the
source commit, source tree hash, artifact manifest hash, scope, approver, and
signature event reference before promotion.
