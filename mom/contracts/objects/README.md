Each subdirectory contains an authored contract package for a canonical ERP+MOM
object. The package file is `contract.json` and is consumed by
`mom/tools/contracts/generate_business_contract_bundle.py`.

These packages are the place to make business meaning explicit when registry
artifacts alone are too ambiguous for humans or AI tools.
