# Tranche 13 Unresolved Backlog Ledger

Date: 2026-04-14

This ledger resolves the six first-pass agent reports against current code, tests, generated artifacts, and local verifier output. Default disposition for code-fixable items is `FIX_NOW`.

## Ledger

| Item | Source prompt / tranche / doc | Original expected outcome | Current verified status | Evidence | Why still open | Code-fixable now | Action required in this run |
|---|---|---|---|---|---|---|---|
| Runtime registry path drift | Tranche 12 registry blocker, commit `a42f0d16`, Agents 1/4/6 | Generated registry artifacts are available to runtime and publication tools | `FIX_NOW -> CLOSED` | Bootstrap files moved from root `data/registry` to consumed `mom/data/registry`; regression test asserts the runtime path and rejects root `data/registry/table-registry.json`; `RegistryService` and canonical spine keep validation green by overlaying authored contract domain/column metadata when the runtime bootstrap table registry is skeletal | Closed for bootstrap artifacts | Yes | Closed in this run |
| Bootstrap registry artifact reproducibility | Commit `a42f0d16`, `tools/registry/generate-registry-from-ai-index.py` | Committed bootstrap artifacts are reproducible from the checked-in generator | `FIX_NOW -> CLOSED` | `tools/registry/generate-registry-from-ai-index.py` writes to `mom/data/registry`; generated table registry exposes `primaryKey`/`primaryKeys`; stale root artifacts removed | Closed for bootstrap artifact layer | Yes | Closed in this run |
| Publication truth full closure | Prior publication truth prompts, `verify_publication_truth.py` | Full publication artifact set passes all gates | `BLOCKED_EXTERNAL_SOURCE_INPUTS` after path fix | `verify_publication_truth.py` still fails Gate A; real orchestrator now finds `table-registry.json` but fails on absent `data-fields.json`, `orphan-resolution.json`, `domain-architecture.json`, wave policy files, `endpoint-catalog.json`, and `frontend-foundation-catalog.json` | Required controlled source inputs are absent from the repo | No safe local fix without source/domain inputs | Record precise blockers and avoid green claims |
| Publication verifier traceback on missing artifacts | Tranche 13 focused verification | Missing artifacts should fail deterministically, not crash in later gates | `FIX_NOW -> CLOSED` | `verify_publication_truth.py` now reports missing required artifacts, marks the artifact set incomplete, skips publication-dependent gates, and returns non-zero without `FileNotFoundError` | Closed locally | Yes | Closed in this run |
| Operational blind-spot generator traceback on missing catalog | Tranche 13 focused verification | Missing source catalogs should be reported as explicit source-input blockers | `FIX_NOW -> CLOSED` | `generate_operational_blind_spot_report.py` now exits with `Missing required registry input: ...` instead of a Python traceback when `operational-blind-spot-catalog.json` is absent | Closed locally | Yes | Closed in this run |
| Schema-authority refresh wrapper fatal on required generator failure | Tranche 13 focused verification | Missing-input publication blockers should be visible as command failures, not PHP fatal traces | `FIX_NOW -> CLOSED` | `refresh_data_schema_authority.php --skip-publication` now exits non-zero with `[refresh_data_schema_authority] required command failed: ...` after the explicit missing-input line | Closed locally | Yes | Closed in this run |
| Schema authority summary counted partial registry as schema table authority | Tranche 13 focused verification | Schema authority summary should not downgrade authoritative table count to partial bootstrap registry count | `FIX_NOW -> CLOSED` | `refresh_schema_authority_summary.php` now reports `table_count=772` from `schema.sql`, `registry_table_count=661` separately, and marks the registry as bootstrap/partial when counts differ | Closed locally | Yes | Closed in this run |
| Configured-but-unverified Loki treated healthy at controller layer | Tranche 13 pass-2 Agent 2/5 | Health readiness must not treat configured logging transport as healthy until Loki has been verified by a successful push | `FIX_NOW -> CLOSED` | `HealthController::componentHealthy()` now fails configured Loki unless both `loki_available` and `loki_verified` are true; `HealthControllerRuntimeAuthorityTest` asserts default unverified logging is not ready | Closed locally | Yes | Closed in pass-2 fix cycle |
| Metadata-only blocker families | `canonical_publication_orchestrator.py`, Agent 6 | Generator closes timestamp, operation, execution status, planning, resource, traceability metadata gaps | `BLOCKED_BY_MISSING_REGISTRY_INPUTS` after path fix | The orchestrator reaches generator steps but stops on missing `data-fields.json`, `orphan-resolution.json`, `domain-architecture.json`, and policy files before metadata closure can run | Mechanical closure depends on absent source inputs | No safe local fix without the missing registry source set | Record blocker; do not fabricate metadata closures |
| DB_PASSWORD-only blocker claim | Tranche 12 pass-2 notes | Schema publication blocked only by DB credential | `INVALID_CLAIM` | Current exercised failure reaches missing registry inputs before credentials are decisive | Root cause changed after bootstrap artifact landing | No | Correct docs so registry input/path drift is the current blocker |
| Runtime authority aggregate overconfidence | Agent 4 | Health surface should not hide partial or compatibility-only authority slices | `FIX_NOW -> CLOSED` | `RuntimeAuthorityService` now reports `non_authoritative_slices`, `mixed_authority`, and `strict_authority_ready`; HealthController exposes `runtime_authority_strict` | Closed locally with focused tests | Yes | Closed in this run |
| Repo hygiene: tracked ignored/local artifacts | Tranche 13 repo hygiene rule, local `git ls-files` checks | Source tree excludes OS/editor/runtime/temp debris | `FIX_NOW -> CLOSED` | Removed tracked `.DS_Store`, local settings, log/pid files, `mom/docs/tmp/*`, and `.tmp-benchmark-schema.sql`; `.gitignore` now covers `.pid` and temp SQL artifacts | Closed locally | Yes | Closed in this run |
| Ignored runtime residue from verification runs | Agent 6, local `git status --ignored=matching` | Verification outputs do not masquerade as source authority | `FIX_NOW -> CLOSED` | Runtime residue was removed or kept ignored after verifier runs; only selected bootstrap JSON files are unignored under `mom/data/registry` | Closed locally | Yes | Clean before final merge gate |
| Attachment contract semantics | Orchestrator blocker classes, Agents 3/6 | Attachment semantics are governed by domain contract | `PRODUCT_DECISION_REQUIRED` | Classified as manual semantic contract | Needs product/domain owner | No | Keep explicit product-decision blocker |
| Work-instruction signal semantics | Orchestrator blocker classes, Agents 3/6 | Work-instruction signal semantics are governed | `PRODUCT_DECISION_REQUIRED` | Classified as manual semantic contract | Needs product/domain owner | No | Keep explicit product-decision blocker |
| Formula / aggregate contract semantics | Orchestrator blocker classes, Agents 3/6 | Formula/aggregate contract semantics are governed | `PRODUCT_DECISION_REQUIRED` | Classified as manual semantic contract | Needs product/domain owner | No | Keep explicit product-decision blocker |
| Full admin-tab authoritative persistence migration | Admin-tab tranche docs, Agents 1/6 | Admin control plane is fully backend-authoritative | `PRODUCT_DECISION_REQUIRED` | Alias/direct-write issues are closed; deeper migration scope remains broad | Requires product/architecture decision | No | Do not fake completion |

## Blocked external / source inputs

- Full publication truth remains blocked by missing controlled registry source inputs: `data-fields.json`, `orphan-resolution.json`, `domain-architecture.json`, wave governance policies, operational blind/stress catalogs, `endpoint-catalog.json`, and `frontend-foundation-catalog.json`.
- `DB_PASSWORD` is not the first active blocker in the current tree.

## Product decisions

- Attachment contract semantics.
- Work-instruction signal semantics.
- Formula / aggregate contract semantics.
- Full admin-tab authoritative persistence migration scope.
- Multisite/site-scoped readiness thresholds.
