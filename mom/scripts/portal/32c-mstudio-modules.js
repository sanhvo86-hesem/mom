/* 32c-mstudio-modules.js — Module Studio P3 stub (modules + reference surface extension).
 * Replaced by P3 session with the real Modules surface implementation.
 * When loaded, overrides MStudio.api.openModuleContent with the real Lego Assemble entry. */
(function () { 'use strict'; /* placeholder — P3 registers modules + reference surfaces here */ })();
